---
phase: 04-quiz-one-question-flow
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - utils/quiz_data.py
  - routes/quiz.py
  - tests/quizflow/test_question_set_stability.py
  - tests/quizflow/test_quiz_submission_contract.py
autonomous: true
requirements:
  - QUIZ-04
  - QUIZ-01
  - QUIZ-02
  - QUIZ-03
must_haves:
  truths:
    - "Question bank includes expanded cybersecurity/scam themes suitable for one-question flow."
    - "Each attempt uses a stable, normalized question set from start to submit."
    - "Final submit still writes one QuizResult and preserves result/certificate session contract."
  artifacts:
    - path: "utils/quiz_data.py"
      provides: "Expanded and normalized quiz question bank with topic metadata"
    - path: "tests/quizflow/test_question_set_stability.py"
      provides: "Automated checks for question-set freeze and topic completeness"
    - path: "tests/quizflow/test_quiz_submission_contract.py"
      provides: "Automated checks for score persistence and certificate/result redirects"
  key_links:
    - from: "routes/quiz.py"
      to: "utils/quiz_data.py"
      via: "attempt initialization normalizes source questions before session freeze"
      pattern: "topic|questions|quiz_attempt"
    - from: "routes/quiz.py"
      to: "models/QuizResult"
      via: "final submit persistence"
      pattern: "QuizResult and db session commit path"
---

<objective>
Finalize quiz flow by expanding topic-ready questions and hardening end-to-end submission compatibility.
Purpose: Complete QUIZ-04 while protecting QUIZ-01..03 behaviors already delivered in Plans 01-02.
Output: Topic-extended question bank, stable attempt initialization, and submission-contract regression tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quiz-one-question-flow/4-CONTEXT.md
@.planning/phases/04-quiz-one-question-flow/04-RESEARCH.md
@.planning/phases/04-quiz-one-question-flow/04-VALIDATION.md
@utils/quiz_data.py
@routes/quiz.py
@templates/quiz_result.html
@templates/certificate.html

<interfaces>
Question record minimum contract:
```python
{
  "id": int,
  "question": str,
  "options": list[str],
  "answer": int,
  "topic": str,
}
```

Submission compatibility contract:
```python
session["last_quiz_score"]
session["max_quiz_score"]
session["certificate_code"]
QuizResult(name, email, score, max_score, certificate_code?)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stability and submission-contract regression tests for final quiz flow</name>
  <read_first>
    - .planning/phases/04-quiz-one-question-flow/04-VALIDATION.md
    - routes/quiz.py
    - utils/quiz_data.py
  </read_first>
  <files>tests/quizflow/test_question_set_stability.py, tests/quizflow/test_quiz_submission_contract.py</files>
  <behavior>
    - Test 1: Attempt question IDs remain unchanged across refresh/back within one attempt.
    - Test 2: Every served question has required normalized fields including topic.
    - Test 3: Final submit creates one `QuizResult`, sets score session keys, and routes to result/certificate correctly.
  </behavior>
  <action>Create integration tests covering question-set freeze + topic schema validation + final submission compatibility. Include both pass and fail scoring branches to assert redirect/session contracts.</action>
  <verify>
    <automated>python -m unittest tests.quizflow.test_question_set_stability tests.quizflow.test_quiz_submission_contract -v</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if question topic metadata is missing.
    - Tests fail if result/certificate session keys regress.
  </acceptance_criteria>
  <done>End-to-end stability and submission contract checks exist and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Expand topic-based quiz bank and normalize attempt ingestion path</name>
  <read_first>
    - utils/quiz_data.py
    - routes/quiz.py
    - tests/quizflow/test_question_set_stability.py
    - tests/quizflow/test_quiz_submission_contract.py
  </read_first>
  <files>utils/quiz_data.py, routes/quiz.py</files>
  <action>Extend `quiz_questions` with explicit cybersecurity/scam topics and ensure each record meets normalized schema. In route attempt initialization, sanitize and normalize question payload before storing in session (`id`, `question`, `options`, `answer`, `topic`, optional `is_ai`). Keep final scoring threshold and certificate generation behavior backward compatible with existing result/certificate pages.</action>
  <verify>
    <automated>python -m unittest tests.quizflow.test_question_set_stability tests.quizflow.test_quiz_submission_contract -v && python -m unittest discover -s tests/quizflow -p "test_*.py" -v</automated>
  </verify>
  <acceptance_criteria>
    - Expanded question bank is topic-tagged and consumable by one-question flow.
    - Attempt payload remains compact and stable across session lifecycle.
    - Full phase quizflow test suite passes.
  </acceptance_criteria>
  <done>QUIZ-04 delivered with stable data contract and no regression in submit/result/certificate flow.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.quizflow.test_question_set_stability tests.quizflow.test_quiz_submission_contract -v
- python -m unittest discover -s tests/quizflow -p "test_*.py" -v
</verification>

<success_criteria>
- QUIZ-04 question-bank expansion is complete and normalized.
- QUIZ-01..03 remain green under full `tests/quizflow` suite.
- Final submit contract stays compatible with existing quiz result and certificate pages.
</success_criteria>

<output>
After completion, create `.planning/phases/04-quiz-one-question-flow/04-03-SUMMARY.md`
</output>
