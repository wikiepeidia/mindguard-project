---
phase: 26-auth-flow-smtp-cutover
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/auth.py
  - tests/test_otp_auth_integration.py
autonomous: true
requirements:
  - SMTPC-01
  - SMTPC-02
  - SMTPC-03
  - SMTPO-02
must_haves:
  truths:
    - Register flow works when SMTP is the selected provider.
    - Resend flow works when SMTP is the selected provider.
    - SMTP failures keep existing OTP challenge/session safety rules intact.
    - Operators can distinguish SMTP misconfiguration from transient failures through route diagnostics.
  artifacts:
    - path: routes/auth.py
      provides: SMTP-aware failure diagnostics without changing route/provider boundaries
    - path: tests/test_otp_auth_integration.py
      provides: SMTP-path register/resend integration coverage
  key_links:
    - from: routes/auth.py
      to: services/otp_email_delivery.py
      via: send_otp_email and otp_email_delivery_status
      pattern: send_otp_email|otp_email_delivery_status
    - from: tests/test_otp_auth_integration.py
      to: services/otp_email_delivery.py
      via: patched mail.send transport under EMAIL_PROVIDER=smtp
      pattern: services\.otp_email_delivery\.mail\.send
---

<objective>
Cut the register/resend auth flows over to the SMTP provider path with route-level regression coverage and operator diagnostics.

Purpose: Prove the real auth entrypoints work under `EMAIL_PROVIDER=smtp` while preserving the fail-closed resend/session contract from earlier OTP phases.
Output: Route diagnostics plus SMTP-specific auth integration tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-auth-flow-smtp-cutover/26-CONTEXT.md
@routes/auth.py
@services/otp_email_delivery.py
@tests/test_otp_auth_integration.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add operator-facing SMTP failure diagnostics in auth flows</name>
  <files>routes/auth.py</files>
  <action>Per D-01 and D-03, keep auth routes calling `send_otp_email()` but add structured diagnostics when register or resend delivery fails. The diagnostics must distinguish provider category and missing config without surfacing raw secrets to end users. Preserve the existing user-facing flashes and fail-closed behavior.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -q</automated>
  </verify>
  <done>Register and resend failures log enough structured context for operators to tell misconfiguration apart from transient SMTP/runtime failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SMTP-path register and resend route regression coverage</name>
  <files>tests/test_otp_auth_integration.py</files>
  <behavior>
    - Test 1: register succeeds and redirects to verify when `EMAIL_PROVIDER=smtp` and the SMTP transport succeeds.
    - Test 2: register fails closed and logs `misconfigured` when the SMTP config is incomplete.
    - Test 3: resend succeeds with SMTP and updates the pending challenge/session.
    - Test 4: resend timeout keeps the current challenge/session and logs a transient failure category.
  </behavior>
  <action>Exercise the real SMTP branch by setting `EMAIL_PROVIDER=smtp` and patching `services.otp_email_delivery.mail.send` instead of patching `routes.auth.send_otp_email`. Reuse the current auth/session rules from Phase 24 and verify they still hold under the SMTP cutover.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -q</automated>
  </verify>
  <done>SMTP-specific auth integration tests prove register/resend success and failure behavior under the actual provider boundary used in production.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth routes -> provider boundary | Incorrect SMTP cutover could corrupt pending OTP state or treat transport failure as success |
| runtime config -> operator diagnostics | Missing config could be misread as transient provider failure if route diagnostics are too vague |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T | `routes/auth.py` register/resend failure handling | mitigate | Keep fail-closed route behavior and add SMTP-path regression tests for register and resend |
| T-26-02 | I | operator diagnostics | mitigate | Log provider category and missing non-secret config keys when delivery fails |
| T-26-03 | E | resend/session lifecycle | mitigate | Assert current challenge/session preservation on SMTP resend timeout |
</threat_model>

<verification>
Run `python -m pytest tests/test_otp_auth_integration.py -q` and confirm the new SMTP-path register/resend tests pass with the existing OTP suite.
</verification>

<success_criteria>
The auth entrypoints behave correctly under `EMAIL_PROVIDER=smtp`, and operator diagnostics can separate SMTP misconfiguration from transient runtime failures.
</success_criteria>

<output>
After completion, create `.planning/phases/26-auth-flow-smtp-cutover/26-01-SUMMARY.md`
</output>
