---
phase: 08-app-startup-cleanup-data-seeding
plan: 01
type: execute
wave: 1
depends_on: ["07-01"]
files_modified: ["app.py", "database/seed_all.py"]
autonomous: true
requirements: [START-01, START-03, SEED-01]
must_haves:
  truths:
    - "app.py does not call db.create_all() or any seed function on import"
    - "All 13 tables exist in NeonDB (confirmed via information_schema)"
    - "Seed data (admin, users, scammer reports, leaderboard, articles, quiz results) present after one run"
  artifacts:
    - path: "app.py"
      provides: "Clean startup — no cold-start seeding"
    - path: "database/seed_all.py"
      provides: "Idempotent seeder with check-before-insert guards"
  key_links:
    - from: "database/seed_all.py"
      to: "NeonDB tables"
      via: "filter_by().first() idempotency guards"
---

<objective>
Clean up app.py startup (remove db.create_all and IS_VERCEL seed blocks) and run
the seed script once against NeonDB to populate all tables.

Purpose: Ensure Vercel cold starts don't try to re-seed, and NeonDB has data for the app.
Output: Clean app.py + populated NeonDB
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-postgresql-configuration-connection/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify app.py cleanup and run seed</name>
  <files>app.py, database/seed_all.py</files>
  <action>
    D-01 (per 08-CONTEXT.md): Remove IS_VERCEL seed block and legacy verification_status fix. Already done.
    D-02 (per 08-CONTEXT.md): Remove db.create_all(). Already done.
    D-03 (per 08-CONTEXT.md): Add idempotency guards to seed functions. Already done.
    D-04: Run python -m database.seed_all to populate NeonDB tables.
  </action>
  <verify>
    <automated>python -m database.seed_all</automated>
  </verify>
  <done>All 6 seed functions complete, NeonDB has admin + 8 users + 20 reports + 17 leaderboard + 8 articles + 15 quiz results</done>
</task>

</tasks>

<verification>
- app.py: no db.create_all(), no IS_VERCEL seed block
- NeonDB: 13/13 tables exist, all populated
- Seed is idempotent (re-run shows "skipping" messages, not duplicates)
</verification>

<success_criteria>

1. app.py nimports cleanly with no side effects
2. python -m database.seed_all reports all records present or skipped
3. NeonDB registrations > 0, scammer_reports > 0
</success_criteria>
