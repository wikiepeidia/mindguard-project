---
phase: 21-production-otp-email-delivery
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - routes/auth.py
  - templates/verify_otp.html
autonomous: true
requirements: [OTPMAIL-01, OTPMAIL-02]
must_haves:
  truths:
    - Register flow sends OTP through configured Resend provider before verify step.
    - If OTP email send fails, account is not activated and user receives clear retry guidance.
    - User-facing verify flow keeps pending email context without exposing OTP values.
  artifacts:
    - path: routes/auth.py
      provides: OTP delivery integration and fail-closed error handling in register flow
    - path: templates/verify_otp.html
      provides: Clear OTP guidance copy for delivery and retry behavior
  key_links:
    - from: routes/auth.py
      to: services/otp_email_delivery.py
      via: send_otp_email call and result mapping
      pattern: send_otp_email
    - from: routes/auth.py
      to: models/models.py
      via: OtpChallenge state handling on delivery failure
      pattern: OtpChallenge
---

<objective>
Integrate Resend OTP delivery into registration flow and enforce fail-closed behavior when delivery fails.

Purpose: Deliver real OTP email in production while preserving secure account activation semantics.
Output: Auth register flow that sends OTP and handles delivery failures safely and clearly.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-production-otp-email-delivery/21-CONTEXT.md
@.planning/phases/21-production-otp-email-delivery/21-01-PLAN.md
@routes/auth.py
@templates/verify_otp.html
@services/otp_email_delivery.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire register flow to Resend send_otp_email contract</name>
  <read_first>routes/auth.py, services/otp_email_delivery.py, utils/otp_security.py</read_first>
  <files>routes/auth.py</files>
  <behavior>
    - Register issues challenge then sends OTP through service with timeout/retry policy.
    - Success path stores pending session keys and redirects to verify page.
    - Delivery path does not reintroduce hardcoded OTP behavior.
  </behavior>
  <action>Update register() in routes/auth.py to call `send_otp_email` with issued plaintext OTP and contextual metadata. On success, keep existing pending session contract (`pending_registration`, `pending_otp_challenge_id`, `pending_verification_email`) and redirect to verify page. Ensure no OTP plaintext is flashed or logged. Keep route logic deterministic and readable for future resend integration.</action>
  <verify>
    <automated>python -m py_compile routes/auth.py</automated>
  </verify>
  <done>Register path delivers OTP through Resend service and preserves secure pending state on success.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fail-closed handling and user retry guidance on send failure</name>
  <read_first>routes/auth.py, templates/verify_otp.html</read_first>
  <files>routes/auth.py, templates/verify_otp.html</files>
  <behavior>
    - Failed send prevents activation flow from continuing.
    - Pending challenge/session state is cleaned or invalidated consistently on failure.
    - User sees clear Vietnamese retry guidance without provider internals.
  </behavior>
  <action>Handle non-ok result from `send_otp_email` by failing closed: do not proceed to verify onboarding path unless a send success is confirmed. Invalidate or discard newly issued challenge/session pending state to avoid dangling active challenges on send failure. Return user to register with actionable guidance message. Update verify_otp template copy if needed to align with production-delivery wording and avoid ambiguity about email status.</action>
  <verify>
    <automated>python -m py_compile routes/auth.py && python -m pytest tests/test_otp_auth_integration.py -k "register"</automated>
  </verify>
  <done>Delivery failure never activates account and user guidance is clear, safe, and actionable.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user register input -> OTP send flow | Untrusted input enters challenge issuance and provider send pipeline |
| delivery result -> account/session state | External provider outcome controls whether verification flow may continue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | T | Register state transition after send failure | mitigate | Fail closed and clear/invalidate pending challenge when send fails |
| T-21-05 | I | User-facing error leakage | mitigate | Surface generic retry guidance, hide provider internals and stack traces |
| T-21-06 | R | Ambiguous delivery outcomes | mitigate | Use structured send result mapping with explicit categories and route decisions |
</threat_model>

<verification>
- Compile route/template changes.
- Run OTP auth integration register-focused tests.
</verification>

<success_criteria>

- OTP email is sent by Resend provider in register path.
- Failed send does not allow activation or ambiguous verify continuation.
- UX copy communicates retry guidance clearly in Vietnamese.
</success_criteria>

<output>
After completion, create `.planning/phases/21-production-otp-email-delivery/21-02-SUMMARY.md`
</output>
