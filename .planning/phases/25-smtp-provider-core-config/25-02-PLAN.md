---
phase: 25-smtp-provider-core-config
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - tests/test_otp_email_delivery.py
autonomous: true
requirements:
  - SMTPP-02
  - SMTPP-03
must_haves:
  truths:
    - SMTP config validation is locked by automated tests.
    - SMTP delivery outcome mapping is locked by automated tests.
    - Fail-closed categories remain stable for future refactors.
  artifacts:
    - path: tests/test_otp_email_delivery.py
      provides: SMTP provider config and outcome coverage
  key_links:
    - from: tests/test_otp_email_delivery.py
      to: services/otp_email_delivery.py
      via: direct status/send assertions
      pattern: otp_email_delivery_status|send_otp_email
---

<objective>
Lock the new SMTP provider behavior behind deterministic unit coverage.

Purpose: Prevent future refactors from weakening SMTP readiness validation or delivery failure mapping after the Resend pivot work lands.
Output: Expanded delivery-provider unit tests covering SMTP config and normalized outcomes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-smtp-provider-core-config/25-CONTEXT.md
@services/otp_email_delivery.py
@tests/test_otp_email_delivery.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SMTP config readiness coverage</name>
  <files>tests/test_otp_email_delivery.py</files>
  <behavior>
    - Test 1: `smtp` provider reports ready when host, auth, sender, and TLS/SSL choices are valid.
    - Test 2: `smtp` provider reports `misconfigured` when required SMTP settings are missing.
    - Test 3: `smtp` provider reports `misconfigured` when TLS and SSL are both enabled.
  </behavior>
  <action>Add focused status tests that prove the SMTP config contract introduced in Plan 01 is validated deterministically and fails closed before any real transport call occurs.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_email_delivery.py -q</automated>
  </verify>
  <done>The delivery status suite proves which SMTP configs are accepted versus rejected before send.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SMTP delivery outcome coverage</name>
  <files>tests/test_otp_email_delivery.py</files>
  <behavior>
    - Test 1: SMTP success returns `sent` and builds a message with sender and recipient data.
    - Test 2: SMTP auth/provider failures map to `provider_rejected`.
    - Test 3: socket timeout maps to `timeout` and network failures map to `network_error`.
  </behavior>
  <action>Extend the same unit module with injected SMTP transports so success and failure categories are verified without requiring a live mailbox server. Preserve the existing Resend tests while expanding the suite for SMTP.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_email_delivery.py -q</automated>
  </verify>
  <done>SMTP delivery outcome mapping is enforced by unit tests and cannot regress silently.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness -> provider contract | Missing regression coverage could let future OTP transport changes weaken fail-closed behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | T | `tests/test_otp_email_delivery.py` coverage gap | mitigate | Add deterministic SMTP status and outcome tests for accepted/rejected/timeout/network paths |
| T-25-05 | E | future transport refactors | mitigate | Keep the SMTP contract locked with direct assertions against normalized categories |
</threat_model>

<verification>
Run `python -m pytest tests/test_otp_email_delivery.py -q` and confirm SMTP status and send-outcome assertions all pass.
</verification>

<success_criteria>
The SMTP provider contract is proven by unit tests covering readiness, success, rejection, timeout, and network failure outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/25-smtp-provider-core-config/25-02-SUMMARY.md`
</output>
