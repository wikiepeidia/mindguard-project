---
phase: 15-conventions-redaction-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - docs/technical/CONVENTIONS.md
autonomous: true
requirements:
  - CONV-01
  - CONV-02
must_haves:
  truths:
    - ".env.example tồn tại với tất cả env vars từ config.py và giá trị placeholder"
    - "docs/technical/CONVENTIONS.md tồn tại với quy ước ngôn ngữ Việt-Anh"
    - "CONVENTIONS.md chứa bảng thuật ngữ (glossary) ~20-30 thuật ngữ kỹ thuật"
    - "CONVENTIONS.md chứa redaction rules ngăn secrets xuất hiện trong tài liệu"
    - ".env.example KHÔNG chứa bất kỳ giá trị secret thật nào"
  artifacts:
    - path: ".env.example"
      provides: "Environment variable reference for Vercel deployment"
      contains: "DATABASE_URL"
    - path: "docs/technical/CONVENTIONS.md"
      provides: "Document writing conventions, glossary, and redaction rules"
      contains: "Bảng thuật ngữ"
  key_links:
    - from: "docs/technical/CONVENTIONS.md"
      to: "config.py"
      via: "Redaction rules reference known secret patterns from config.py"
      pattern: "SECRET_KEY|ADMIN_PASSWORD|ENCRYPTION_KEY"
    - from: ".env.example"
      to: "config.py"
      via: "Lists all os.environ.get() keys from config.py"
      pattern: "DATABASE_URL|OPENROUTER_API_KEY|CLOUDFLARE"
---

<objective>
Create `.env.example` and `docs/technical/CONVENTIONS.md` to establish documentation
conventions and secret protection rules before any documentation writing begins.

Purpose: All subsequent documentation phases (16-19) will reference these conventions
for consistent language, terminology, and secret handling.

Output: Two files — `.env.example` (CONV-01) and `docs/technical/CONVENTIONS.md` (CONV-02).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-conventions-redaction-setup/CONTEXT.md
@config.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .env.example with all environment variables</name>
  <files>.env.example</files>
  <read_first>
    - config.py (source of truth — all os.environ.get() calls)
    - .gitignore (verify .env.example won't be ignored)
  </read_first>
  <action>
Create `.env.example` in the project root with all environment variables extracted from `config.py`.

Structure the file with commented sections grouping related vars. Use `your-xxx-here` placeholder format for secrets and actual default values for non-secret config.

Content must be exactly:

```
# MindGuard v2 — Environment Variables
# Copy this file and fill in real values for your deployment.
# On Vercel: set these in Project Settings > Environment Variables.
# For local dev: you can also use .env/*.json files (see config.py load_local_env).

# ── Flask Core ──────────────────────────────────────────
SECRET_KEY=your-flask-secret-key-here

# ── Database (NeonDB PostgreSQL) ────────────────────────
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# ── AI Chatbot (OpenRouter) ─────────────────────────────
OPENROUTER_API_KEY=sk-or-v1-your-openrouter-key-here

# ── Cloudflare Turnstile (CAPTCHA) ──────────────────────
CLOUDFLARE_SITE_KEY=your-cloudflare-site-key-here
CLOUDFLARE_SECRET_KEY=your-cloudflare-secret-key-here

# ── Admin ───────────────────────────────────────────────
ADMIN_PASSWORD=your-admin-password-here
REPORT_ENCRYPTION_KEY=your-encryption-key-here
ADMIN_UNSUSPEND_SECRET=your-unsuspend-secret-here

# ── Anti-Spam Configuration (optional — defaults shown) ─
ABUS_MODE=monitor
ABUS_WINDOW_MINUTES=10
ABUS_THRESHOLD_COUNT=3
ABUS_COOLDOWN_MINUTES=15
ABUS_ACCOUNT_WEIGHT=70
ABUS_COOKIE_WEIGHT=20
ABUS_IP_WEIGHT=10
```

Per D-01: flat Vercel-oriented format, no example JSON files.
  </action>
  <verify>
    <automated>test -f .env.example && grep -c "DATABASE_URL" .env.example && grep -c "OPENROUTER_API_KEY" .env.example && grep -c "SECRET_KEY" .env.example && ! grep -E "sk-or-v1-[a-f0-9]{40}|579c3247|0f27bbb5" .env.example && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - .env.example exists in project root
    - Contains all 16 env vars from config.py: SECRET_KEY, DATABASE_URL, OPENROUTER_API_KEY, CLOUDFLARE_SITE_KEY, CLOUDFLARE_SECRET_KEY, ADMIN_PASSWORD, REPORT_ENCRYPTION_KEY, ADMIN_UNSUSPEND_SECRET, ABUS_MODE, ABUS_WINDOW_MINUTES, ABUS_THRESHOLD_COUNT, ABUS_COOLDOWN_MINUTES, ABUS_ACCOUNT_WEIGHT, ABUS_COOKIE_WEIGHT, ABUS_IP_WEIGHT
    - No real secret values (no "579c3247", no "0f27bbb5", no actual API keys)
    - Has commented section headers grouping related vars
    - Anti-spam vars show default values (monitor, 10, 3, 15, 70, 20, 10)
  </acceptance_criteria>
  <done>.env.example exists with all 16 env vars, placeholder values only, organized by section.</done>
</task>

<task type="auto">
  <name>Task 2: Create docs/technical/CONVENTIONS.md with language conventions, glossary, and redaction rules</name>
  <files>docs/technical/CONVENTIONS.md</files>
  <read_first>
    - config.py (identify secret patterns for redaction rules)
    - .planning/phases/15-conventions-redaction-setup/CONTEXT.md (locked decisions D-02, D-03, D-04)
    - .planning/codebase/CONVENTIONS.md (existing code conventions — avoid duplication, cross-reference only)
  </read_first>
  <action>
Create `docs/technical/CONVENTIONS.md` with three main sections in Vietnamese:

**Header metadata (YAML frontmatter block at top of file):**

owner: MindGuard Team
last_updated: 2026-04-14
source_files: config.py, .planning/codebase/CONVENTIONS.md

**Section 1: Quy ước ngôn ngữ (Language Conventions)**

- Prose viết bằng tiếng Việt
- Thuật ngữ kỹ thuật giữ nguyên tiếng Anh (in nghiêng hoặc `code`)
- Code blocks, file paths, command lines giữ nguyên tiếng Anh
- Tên riêng (MindGuard, Flask, NeonDB, Vercel, OpenRouter) giữ nguyên
- Tiêu đề có thể song ngữ: "Kiến trúc hệ thống (Architecture)"

**Section 2: Bảng thuật ngữ (Glossary)**
Table with columns: Thuật ngữ (English) | Giải thích (Vietnamese) | Ghi chú

Include ~25 terms covering:

- Framework: blueprint, route, endpoint, middleware, decorator, template, static files
- Database: model, migration, schema, query, relationship, foreign key, ORM
- Deployment: serverless, cold start, environment variable, secret, deployment, rollback
- Security: rate limiting, CAPTCHA, CSRF, session, authentication, encryption
- AI: chatbot, prompt, fallback, hard-block, timeout

**Section 3: Quy tắc bảo vệ thông tin (Redaction Rules)**

- KHÔNG BAO GIỜ copy giá trị thật từ config.py, .env/, hoặc Vercel Dashboard vào tài liệu
- Sử dụng format `<PLACEHOLDER>` hoặc `your-xxx-here` cho mọi secret/credential
- Danh sách patterns nguy hiểm cần kiểm tra:
  - `sk-or-v1-*` (OpenRouter API keys)
  - Hex strings dài > 32 ký tự (SECRET_KEY, ADMIN_UNSUSPEND_SECRET)
  - `postgresql://` URLs chứa password thật
  - Cloudflare site/secret keys
- Trước khi commit tài liệu: grep toàn bộ file cho các pattern trên
- Nếu phát hiện secret trong docs: xóa ngay, force-push nếu đã push

**Section 4: Quy tắc cập nhật tài liệu (Document Maintenance)**

- Khi thay đổi route/model/config → cập nhật tài liệu tương ứng
- Bảng mapping: loại thay đổi → file tài liệu cần cập nhật
  - routes/*.py thay đổi → API.md
  - models/models.py thay đổi → DATABASE.md
  - config.py thay đổi → .env.example + ARCHITECTURE.md
  - deploy config thay đổi → SOP Vận hành
- Mỗi tài liệu phải có metadata header: owner, last_updated, source_files

Per D-02: location is docs/technical/CONVENTIONS.md.
Per D-03: minimal glossary ~25 terms.
Per D-04: docs-only redaction rules, no code changes.
  </action>
  <verify>
    <automated>test -f docs/technical/CONVENTIONS.md && grep -c "Bảng thuật ngữ" docs/technical/CONVENTIONS.md && grep -c "Redaction\|bảo vệ thông tin" docs/technical/CONVENTIONS.md && grep -c "blueprint" docs/technical/CONVENTIONS.md && grep -c "cập nhật tài liệu" docs/technical/CONVENTIONS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/technical/CONVENTIONS.md exists
    - Contains "Quy ước ngôn ngữ" section with Vietnamese-English rules
    - Contains "Bảng thuật ngữ" section with table of ~20-30 terms
    - Glossary table includes: blueprint, route, endpoint, serverless, cold start, rate limiting, CAPTCHA, ORM, migration
    - Contains redaction rules section mentioning: sk-or-v1, SECRET_KEY, postgresql://, PLACEHOLDER
    - Contains document maintenance section with change-to-doc mapping table
    - Has metadata header with owner, last_updated, source_files
    - All prose is in Vietnamese, technical terms in English
  </acceptance_criteria>
  <done>docs/technical/CONVENTIONS.md exists with language conventions, ~25-term glossary, redaction rules, and maintenance rules — all in Vietnamese.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → repo | Documentation files committed to git — must not contain secrets |
| .env.example → developer | Template file read by new team members — must have only placeholders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | .env.example | mitigate | Verify no real secret values via grep check in task verify step; placeholder format enforced |
| T-15-02 | Information Disclosure | CONVENTIONS.md | accept | Docs file contains only rules and glossary — no secrets by design; redaction rules section self-documents the risk |
</threat_model>

<verification>
1. `.env.example` exists and contains all 16 env vars from config.py
2. `.env.example` contains NO real secret values (grep for known patterns)
3. `docs/technical/CONVENTIONS.md` exists with all 4 sections
4. Glossary has ~20-30 terms
5. Redaction rules list known danger patterns
6. Both files are in Vietnamese (prose) with English technical terms
</verification>

<success_criteria>

- CONV-01 satisfied: `.env.example` lists all env vars with placeholders
- CONV-02 satisfied: `docs/technical/CONVENTIONS.md` has language conventions + glossary + redaction rules
- Phase 15 Success Criteria 1-3 from ROADMAP.md all met
</success_criteria>

<output>
After completion, create `.planning/phases/15-conventions-redaction-setup/15-01-SUMMARY.md`
</output>
