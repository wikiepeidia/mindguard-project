---
phase: 17-system-documents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/technical/ARCHITECTURE.md
autonomous: true
requirements:
  - TECH-01
must_haves:
  truths:
    - "ARCHITECTURE.md phản ánh đúng stack hiện tại: Flask + NeonDB PostgreSQL + Vercel serverless"
    - "Có ít nhất 2 Mermaid diagrams: system overview flowchart và request flow sequence diagram"
    - "Các section Data Flow, Infrastructure, Known Constraints cập nhật cho Vercel + NeonDB"
    - "Cross-references đến DATABASE.md và API.md"
  artifacts:
    - path: "docs/technical/ARCHITECTURE.md"
      provides: "Complete architecture document for current stack"
      contains: "NeonDB"
  key_links:
    - from: "docs/technical/ARCHITECTURE.md"
      to: "docs/technical/DATABASE.md"
      via: "Markdown link trong See Also section"
    - from: "docs/technical/ARCHITECTURE.md"
      to: "docs/technical/API.md"
      via: "Markdown link trong See Also section"
---

<objective>
Rewrite ARCHITECTURE.md để phản ánh đúng stack hiện tại (NeonDB PostgreSQL + Vercel serverless) với 2 Mermaid diagrams và cross-references.

Purpose: Team member mới hiểu kiến trúc tổng thể từ tài liệu mà không cần đọc source code.
Output: `docs/technical/ARCHITECTURE.md` cập nhật hoàn chỉnh.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-system-documents/17-CONTEXT.md

Key source files:
@app.py — Flask app setup, blueprint registration, security headers
@config.py — Configuration with NeonDB PostgreSQL + OpenRouter + Cloudflare
@extensions.py — db, mail, limiter, csrf initialization
@models/models.py — 14 SQLAlchemy models
@docs/technical/CONVENTIONS.md — Vietnamese prose, glossary, redaction rules
@docs/technical/DATABASE.md — Current database reference (for cross-refs)
@docs/technical/DECISIONS.md — ADRs (for cross-refs)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite ARCHITECTURE.md cho NeonDB + Vercel stack</name>
  <files>docs/technical/ARCHITECTURE.md</files>
  <action>
Rewrite ARCHITECTURE.md following these decisions:

**Per D-01 — Selective rewrite:**

1. Update METADATA comment: owner @systems-architect, update trigger, last updated date.
2. **Overview section**: Update từ "monolithic Flask web application using server-side rendering" sang mô tả đúng: modular Flask monolith deployed as Vercel serverless functions, NeonDB PostgreSQL, OpenRouter AI.
3. **Tech Stack table**: Replace SQLite → NeonDB PostgreSQL 15 (serverless), localhost+ngrok → Vercel (mindguard-five.vercel.app), add Vercel CLI row. Remove ngrok row.
4. **System Components > Frontend Architecture**: Mostly correct, keep. Minor update: remove template hierarchy claim about `templates/quiz/`, `templates/chatbot/` — templates are flat in `templates/`.
5. **System Components > Backend Architecture**: Update blueprint list (add library, api). Add note about `services/admin_guard.py`. Update middleware pipeline to include CSRF (CSRFProtect from flask_wtf).
6. **Infrastructure section**: Replace environments table with Vercel (production: mindguard-five.vercel.app, local: localhost:5000). Remove ngrok. Note CI/CD: not yet configured.

**Per D-02 — 2 Mermaid diagrams:**
7. Add **System Overview** diagram as `flowchart TD`:

   ```
   Browser --> Vercel[Vercel Edge Network]
   Vercel --> Flask[Flask App]
   Flask --> Blueprints[8 Blueprints]
   Blueprints --> Services[Services Layer]
   Blueprints --> Models[SQLAlchemy Models]
   Models --> NeonDB[(NeonDB PostgreSQL)]
   Flask --> External[External Services]
   External --> OpenRouter[OpenRouter AI]
   External --> Cloudflare[Cloudflare Turnstile]
   External --> FlaskMail[Flask-Mail SMTP]
   ```

   Place after Overview text, replacing the ASCII art.

1. Add **Request Flow** diagram as `sequenceDiagram`:

   ```
   Browser->>Vercel: HTTP Request
   Vercel->>Flask: Forward to serverless function
   Flask->>CSRF: Validate token (POST)
   Flask->>Session: Check authentication
   Flask->>Limiter: Rate limit check
   Flask->>Blueprint: Route to handler
   Blueprint->>Service: Business logic
   Service->>DB: Query NeonDB
   DB-->>Service: Result
   Service-->>Blueprint: Response data
   Blueprint->>Jinja2: Render template
   Jinja2-->>Browser: HTML Response
   ```

   Place in new "Request Lifecycle" section after System Components.

2. **Data Flow section**: Fill in the 4 flow descriptions (Auth, Quiz, Scammer Report, Chatbot) with brief text summaries. No additional diagrams needed per D-02.

3. **Design System section**: Remove all placeholder rows (color tokens [#XXXXXX], typography [32px], etc.). Replace with note: "Xem static/css/ cho style definitions. MindGuard sử dụng Bootstrap 5 utility classes kết hợp custom CSS — chưa có formal design token system."

4. **Security Architecture section**: Mostly correct. Add note about CSRF protection (Flask-WTF CSRFProtect). Update "Sessions are stored server-side" — Flask default sessions are cookie-based (client-side, signed).

5. **Performance section**: Replace SQLite notes with NeonDB notes (network latency, connection pooling). Update AI call latency (8s timeout per ADR-004). Note Vercel serverless cold starts.

6. **Known Constraints table**: Replace SQLite rows with Vercel constraints (read-only filesystem, /tmp only, cold starts). Remove ngrok row. Keep no-CI/CD row. Add connection pooling note for NeonDB.

7. **Per D-05 — Add "Xem thêm" section** at bottom linking to DATABASE.md, API.md, DECISIONS.md, CONVENTIONS.md.

8. **Per D-06 — Vietnamese prose**: All descriptive text in Vietnamese. Keep technical terms in English per CONVENTIONS.md glossary.

**IMPORTANT per redaction rules:** Do NOT copy any real secrets from config.py. Use placeholder format from CONVENTIONS.md.
  </action>
  <verify>
    <automated>python -c "
c=open('docs/technical/ARCHITECTURE.md',encoding='utf-8').read()
checks=[
  ('NeonDB' in c, 'NeonDB mentioned'),
  ('Vercel' in c, 'Vercel mentioned'),
  ('flowchart' in c or 'graph TD' in c, 'System overview Mermaid'),
  ('sequenceDiagram' in c, 'Request flow Mermaid'),
  ('DATABASE.md' in c, 'Cross-ref DATABASE.md'),
  ('API.md' in c, 'Cross-ref API.md'),
  ('DECISIONS.md' in c, 'Cross-ref DECISIONS.md'),
  ('sk-or-v1-' not in c and '579c3247' not in c, 'No secrets leaked'),
]
passed=sum(1 for ok,_ in checks if ok)
for ok,name in checks:
  print(f'  {\"PASS\" if ok else \"FAIL\"}: {name}')
print(f'\n{passed}/{len(checks)} checks passed')
assert passed==len(checks), 'Some checks failed'
"</automated>
  </verify>
  <done>ARCHITECTURE.md phản ánh đúng NeonDB + Vercel stack, có 2 Mermaid diagrams, cross-references đầy đủ, không leak secrets.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ output | Documentation only — no executable code, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-17-01 | Information Disclosure | ARCHITECTURE.md | mitigate | Apply CONVENTIONS.md redaction rules — no real secrets in docs |
</threat_model>

<verification>
- ARCHITECTURE.md chứa "NeonDB" và "Vercel" (không còn "SQLite" là stack chính)
- Có 2 Mermaid diagram blocks
- Cross-references đến DATABASE.md, API.md, DECISIONS.md
- Không có secrets (sk-or-v1-, hex keys, real connection strings)
</verification>

<success_criteria>
ARCHITECTURE.md cập nhật đúng stack hiện tại với 2 Mermaid diagrams và cross-references đầy đủ.
</success_criteria>

<output>
After completion, create `.planning/phases/17-system-documents/17-01-SUMMARY.md`
</output>
