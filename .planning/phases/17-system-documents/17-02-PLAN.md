---
phase: 17-system-documents
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/technical/API.md
autonomous: true
requirements:
  - TECH-02
must_haves:
  truths:
    - "API.md liệt kê đầy đủ tất cả routes từ 8 blueprints"
    - "Mỗi route có method, path, auth requirement, response type, mô tả"
    - "Phân loại rõ ràng giữa HTML page routes và JSON API endpoints"
    - "Cross-references đến DATABASE.md và ARCHITECTURE.md"
  artifacts:
    - path: "docs/technical/API.md"
      provides: "Complete API/route reference for all 8 blueprints"
      contains: "Page Routes"
  key_links:
    - from: "docs/technical/API.md"
      to: "docs/technical/DATABASE.md"
      via: "Markdown link trong See Also section"
    - from: "docs/technical/API.md"
      to: "docs/technical/ARCHITECTURE.md"
      via: "Markdown link trong See Also section"
---

<objective>
Rewrite API.md để liệt kê đầy đủ tất cả routes từ 8 blueprints, phân loại HTML vs JSON, với cross-references.

Purpose: Team member mới tra cứu được bất kỳ route nào mà không cần đọc source code.
Output: `docs/technical/API.md` cập nhật hoàn chỉnh.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-system-documents/17-CONTEXT.md

Key source files for route extraction:
@routes/main.py — main_bp (no prefix): /, /leaderboard, /api/search, /scammer/<id>
@routes/auth.py — auth_bp (no prefix): /login, /register, /verify-otp, /onboarding, /complete-onboarding, /profile, /profile/edit, /logout
@routes/quiz.py — quiz_bp (no prefix): /quiz, /quiz/step/<idx>, /quiz/finalize, /quiz/result, /certificate
@routes/scammer.py — scammer_bp (/scammer): /report, /follow
@routes/chatbot.py — chatbot_bp (/chatbot): /, /new, /send, /api, /rename, /support, /feedback
@routes/admin.py — admin_bp (/admin): /login, /, /logout, /unsuspend, /create-admin, /delete-user/<id>, /edit-user/<id>, /scammer-reports, /approve-report/<id>, /reject-report/<id>, /export-dataset, /sensitive-access-logs
@routes/library.py — library_bp (no prefix): /library, /library/<id>
@routes/api.py — api_bp (/api/v1): /check, /stats

@docs/technical/CONVENTIONS.md — Vietnamese prose, glossary, redaction rules
@docs/technical/DATABASE.md — Models for cross-refs
</context>

<interfaces>
<!-- Blueprint registration from app.py -->
Blueprints registered in order: main, scammer, chatbot, quiz, auth, admin, library, api

<!-- URL prefixes from route files -->
main_bp: no prefix
auth_bp: no prefix
quiz_bp: no prefix
library_bp: no prefix
scammer_bp: /scammer
chatbot_bp: /chatbot
admin_bp: /admin
api_bp: /api/v1

<!-- Global rate limit from extensions.py -->
limiter = Limiter(default_limits=["200/minute"])

<!-- Auth patterns from routes -->
session['registration_email'] → user authenticated
session['is_admin'] → admin authenticated
@login_required decorator from utils.helpers
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite API.md với đầy đủ routes từ 8 blueprints</name>
  <files>docs/technical/API.md</files>
  <action>
Rewrite API.md following these decisions:

**Per D-03 — Blueprint-based organization with HTML/JSON sub-groups:**

1. **METADATA comment**: Owner @backend-developer, update trigger route changes, last updated.

2. **Header section**:
   - Title: "API & Route Reference"
   - Base URL: `https://mindguard-five.vercel.app` (production), `http://localhost:5000` (local)
   - Authentication: Flask server-side sessions (cookie-based). Không có Bearer token — xóa template JWT content.
   - Rate limiting: Default 200/min per IP (Flask-Limiter). Per-route limits noted where applicable.

3. **Summary Table** (bảng tổng hợp đầu tài liệu):
   Markdown table with columns: Blueprint | Prefix | Routes | Auth
   List all 8 blueprints with total route count and whether auth required.

4. **Per blueprint section** (8 sections in order: main, auth, quiz, scammer, chatbot, admin, library, api):
   Each blueprint gets an H2 heading with blueprint name and prefix.
   
   Within each blueprint, sub-group:
   - **Page Routes (HTML)** — routes that render templates (return HTML)
   - **API Endpoints (JSON)** — routes that return JSON or redirect-only
   
   If a blueprint only has one type, skip sub-grouping.

5. **Per route listing** (per D-04 — concise format):
   Use Markdown table per sub-group with columns:
   | Method | Path | Auth | Mô tả | Template/Response |
   
   For HTML routes: Template column = template filename
   For JSON endpoints: Response column = key fields in response

6. **Complete route inventory** (extracted from codebase):

   **main_bp** (no prefix):
   - GET / → index.html — Trang chủ, hiển thị thống kê và top bảng xếp hạng
   - GET /leaderboard → leaderboard.html — Bảng xếp hạng người báo cáo
   - POST /api/search → JSON — Tìm kiếm scammer (name/phone/bank)
   - GET /scammer/<int:scammer_id> → scammer_profile.html — Trang chi tiết scammer

   **auth_bp** (no prefix):
   - GET/POST /login → login.html — Đăng nhập
   - GET/POST /register → register.html — Đăng ký tài khoản
   - GET/POST /verify-otp → verify_otp.html — Xác thực OTP qua email
   - GET /onboarding → onboarding.html — Hướng dẫn người dùng mới
   - GET /complete-onboarding → redirect — Hoàn thành onboarding
   - GET /profile → profile.html — Trang cá nhân
   - POST /profile/edit → redirect — Cập nhật thông tin cá nhân
   - GET /logout → redirect — Đăng xuất

   **quiz_bp** (no prefix):
   - GET /quiz → quiz.html — Trang chọn quiz category
   - GET/POST /quiz/step/<int:idx> → quiz_start.html — Làm quiz từng câu
   - GET /quiz/finalize → redirect — Tính điểm và chuyển trang kết quả
   - GET /quiz/result → quiz_result.html — Kết quả quiz
   - GET /certificate → certificate.html — Chứng nhận hoàn thành quiz

   **scammer_bp** (/scammer prefix):
   - GET/POST /scammer/report → report_scammer.html — Gửi báo cáo lừa đảo
   - POST /scammer/follow → JSON — Theo dõi/bỏ theo dõi scammer

   **chatbot_bp** (/chatbot prefix):
   - GET /chatbot/ → chatbot.html — Giao diện chatbot AI
   - GET /chatbot/new → redirect — Tạo phiên chat mới
   - POST /chatbot/send → JSON — Gửi tin nhắn cho chatbot
   - POST /chatbot/api → JSON — API endpoint chatbot (alternative)
   - POST /chatbot/rename → JSON — Đổi tên phiên chat
   - POST /chatbot/support → JSON — Gửi tin hỗ trợ kỹ thuật
   - POST /chatbot/feedback → JSON — Gửi đánh giá phản hồi AI

   **admin_bp** (/admin prefix):
   - GET/POST /admin/login → admin_login.html — Đăng nhập admin
   - GET /admin/ → admin_dashboard.html — Dashboard admin
   - GET /admin/logout → redirect — Đăng xuất admin
   - POST /admin/unsuspend → redirect — Mở khóa tài khoản admin bị suspend
   - POST /admin/create-admin → redirect — Tạo admin mới
   - POST /admin/delete-user/<int:user_id> → redirect — Xóa người dùng
   - POST /admin/edit-user/<int:user_id> → redirect — Sửa thông tin người dùng
   - GET /admin/scammer-reports → admin_scammer_reports.html — Danh sách báo cáo chờ duyệt
   - POST /admin/approve-report/<int:report_id> → redirect — Duyệt báo cáo
   - POST /admin/reject-report/<int:report_id> → redirect — Từ chối báo cáo
   - GET/POST /admin/export-dataset → admin_export.html / file download — Xuất dataset
   - GET /admin/sensitive-access-logs → admin_sensitive_access_logs.html — Xem audit logs

   **library_bp** (no prefix):
   - GET /library → library.html — Danh sách bài viết kiến thức
   - GET /library/<int:article_id> → library_detail.html — Chi tiết bài viết

   **api_bp** (/api/v1 prefix):
   - GET /api/v1/check → JSON — Kiểm tra scammer (public API)
   - GET /api/v1/stats → JSON — Thống kê hệ thống (public API)

7. **Per D-05 — Add "Xem thêm" section** linking to ARCHITECTURE.md, DATABASE.md, DECISIONS.md.

8. **Per D-06 — Vietnamese prose** per CONVENTIONS.md.

**IMPORTANT:** Do NOT include real secrets or API keys. Use placeholder format from CONVENTIONS.md.
  </action>
  <verify>
    <automated>python -c "
c=open('docs/technical/API.md',encoding='utf-8').read()
# Check all 8 blueprints mentioned
bps=['main','auth','quiz','scammer','chatbot','admin','library','api']
bp_ok=sum(1 for b in bps if b in c.lower())
# Check HTML vs JSON classification
has_html='Page Routes' in c or 'HTML' in c
has_json='API Endpoints' in c or 'JSON' in c
# Check cross-refs
has_arch='ARCHITECTURE.md' in c
has_db='DATABASE.md' in c
# Check no secrets
no_secrets='sk-or-v1-' not in c and '579c3247' not in c
# Count route entries (lines with GET/POST)
import re
routes=len(re.findall(r'(GET|POST)',c))
checks=[
  (bp_ok==8, f'All 8 blueprints ({bp_ok}/8)'),
  (has_html, 'HTML route classification'),
  (has_json, 'JSON endpoint classification'),
  (has_arch, 'Cross-ref ARCHITECTURE.md'),
  (has_db, 'Cross-ref DATABASE.md'),
  (no_secrets, 'No secrets leaked'),
  (routes>=30, f'Route count sufficient ({routes} GET/POST mentions)'),
]
for ok,name in checks:
  print(f'  {\"PASS\" if ok else \"FAIL\"}: {name}')
passed=sum(1 for ok,_ in checks if ok)
print(f'\n{passed}/{len(checks)} checks passed')
assert passed==len(checks)
"</automated>
  </verify>
  <done>API.md liệt kê đầy đủ routes từ 8 blueprints, phân loại HTML vs JSON, cross-references đầy đủ, không leak secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ output | Documentation only — no executable code, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-17-02 | Information Disclosure | API.md | mitigate | Apply CONVENTIONS.md redaction rules — no real secrets, no internal-only paths that could aid attackers |
</threat_model>

<verification>
- API.md chứa tất cả 8 blueprints
- Có phân loại HTML page routes vs JSON API endpoints
- Cross-references đến ARCHITECTURE.md, DATABASE.md
- Không có secrets
- Ít nhất 30 route entries
</verification>

<success_criteria>
API.md liệt kê đầy đủ tất cả routes từ 8 blueprints với phân loại HTML vs JSON và cross-references.
</success_criteria>

<output>
After completion, create `.planning/phases/17-system-documents/17-02-SUMMARY.md`
</output>
