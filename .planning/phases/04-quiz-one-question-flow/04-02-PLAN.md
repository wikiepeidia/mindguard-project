---
phase: 04-quiz-one-question-flow
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - templates/quiz.html
  - static/css/quiz.css
  - static/js/quiz.js
  - tests/quizflow/test_quiz_progress_visibility.py
autonomous: true
requirements:
  - QUIZ-02
  - QUIZ-03
must_haves:
  truths:
    - "User can always see current step, total questions, and answered progress while doing the quiz."
    - "Mobile quiz controls remain clear and usable with one-question flow."
    - "Back and refresh behavior communicates stable state instead of confusing resets in a valid session."
  artifacts:
    - path: "templates/quiz.html"
      provides: "One-question layout with explicit progress/status blocks and step actions"
    - path: "static/js/quiz.js"
      provides: "Progress updates and navigation guards aligned with server step state"
    - path: "tests/quizflow/test_quiz_progress_visibility.py"
      provides: "Automated checks for progress/status visibility contract"
  key_links:
    - from: "templates/quiz.html"
      to: "static/js/quiz.js"
      via: "DOM ids/data attributes for progress text and step controls"
      pattern: "progress|current_index|total_questions"
    - from: "templates/quiz.html"
      to: "static/css/quiz.css"
      via: "semantic quiz classes"
      pattern: "quiz-"
---

<objective>
Deliver clear one-question UX signals (progress + status) and keep state messaging coherent during navigation.
Purpose: Satisfy QUIZ-02 and UX side of QUIZ-03 after backend flow is stable.
Output: Updated quiz template/CSS/JS contracts and passing progress visibility tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quiz-one-question-flow/4-CONTEXT.md
@.planning/phases/04-quiz-one-question-flow/04-RESEARCH.md
@.planning/phases/04-quiz-one-question-flow/04-VALIDATION.md
@.planning/phases/03-light-mode-ux-system/03-02-SUMMARY.md
@templates/quiz.html
@static/css/quiz.css
@static/js/quiz.js

<interfaces>
Expected render context from 04-01 route:
```python
render_template(
    "quiz.html",
    current_question=...,
    current_index=...,
    total_questions=...,
    answered_count=...,
    selected_answer=...,
)
```

Template/script contract (required IDs/classes):
```html
<div id="quiz-progress" ...>
<span id="progress-current">...</span>
<span id="progress-total">...</span>
<div id="progress-bar-fill"></div>
</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add progress visibility regression tests for one-question quiz view</name>
  <read_first>
    - .planning/phases/04-quiz-one-question-flow/04-VALIDATION.md
    - templates/quiz.html
    - routes/quiz.py
  </read_first>
  <files>tests/quizflow/test_quiz_progress_visibility.py</files>
  <behavior>
    - Test 1: Quiz page includes explicit current/total progress labels.
    - Test 2: Progress bar width/value changes based on answered_count/current_index.
    - Test 3: Only one question card is active in rendered HTML per step.
  </behavior>
  <action>Create template-contract focused tests for progress/status rendering and one-active-question expectations. Keep assertions deterministic by controlling session attempt payload in test setup.</action>
  <verify>
    <automated>python -m unittest tests.quizflow.test_quiz_progress_visibility -v</automated>
  </verify>
  <acceptance_criteria>
    - Progress tests fail if labels or progress container are removed.
    - Tests do not rely on browser automation and complete quickly.
  </acceptance_criteria>
  <done>Automated QUIZ-02 visibility checks exist and pass.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor quiz UI contract for progress clarity and stable one-question controls</name>
  <read_first>
    - templates/quiz.html
    - static/css/quiz.css
    - static/js/quiz.js
    - tests/quizflow/test_quiz_progress_visibility.py
  </read_first>
  <files>templates/quiz.html, static/css/quiz.css, static/js/quiz.js</files>
  <action>Update quiz template to render one active question with explicit progress label/text/bar and clear step actions (next/back/submit) tied to server state. Align CSS/JS with the new one-question DOM contract; remove stale multi-question selectors (`question-block`, legacy sidebar nav IDs) that no longer apply. Keep all visible UX strings in Vietnamese and maintain Phase 3 light-token styling conventions.</action>
  <verify>
    <automated>python -m unittest tests.quizflow.test_quiz_progress_visibility tests.quizflow.test_state_resume -v</automated>
  </verify>
  <acceptance_criteria>
    - User sees step progress clearly on every step.
    - UI controls do not depend on removed legacy DOM structure.
    - Existing state-resume tests stay green after UI updates.
  </acceptance_criteria>
  <done>Quiz UI exposes clear progress/status and remains consistent with server-managed step state.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.quizflow.test_quiz_progress_visibility tests.quizflow.test_state_resume -v
</verification>

<success_criteria>
- QUIZ-02 is met with deterministic progress/status rendering and tests.
- QUIZ-03 user-facing continuity remains intact after UI refactor.
- One-question UI contract is mobile-safe and light-mode compliant.
</success_criteria>

<output>
After completion, create `.planning/phases/04-quiz-one-question-flow/04-02-SUMMARY.md`
</output>
