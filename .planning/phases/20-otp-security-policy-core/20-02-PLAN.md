---
phase: 20-otp-security-policy-core
plan: 02
type: execute
wave: 2
depends_on: [20-01]
files_modified:
  - routes/auth.py
  - templates/verify_otp.html
autonomous: true
requirements: [OTPSEC-03, OTPPOL-01, OTPPOL-02, OTPPOL-03]
must_haves:
  truths:
    - Expired OTP challenges are rejected using configured TTL policy.
    - Wrong OTP attempts increment counters and trigger temporary lockout.
    - Successful OTP verification is single-use and replay-safe.
    - New OTP issue invalidates prior active challenge for the same registration flow.
  artifacts:
    - path: routes/auth.py
      provides: Secure register/verify OTP lifecycle integration
    - path: templates/verify_otp.html
      provides: OTP verify UI without hardcoded/demo OTP disclosure
  key_links:
    - from: routes/auth.py
      to: models/models.py
      via: OtpChallenge lookup and state updates
      pattern: OtpChallenge
    - from: routes/auth.py
      to: utils/otp_security.py
      via: issue and verify helper calls
      pattern: verify_otp_submission
---

<objective>
Integrate OTP lifecycle controls into the auth register and verify routes, enforcing TTL, attempt lockout, single-use, and challenge invalidation.

Purpose: Move OTP verification from insecure session fallback to policy-enforced server-side challenge state.
Output: Secure auth routes and verify template aligned with Phase 20 OTP policy.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-otp-security-policy-core/20-RESEARCH.md
@.planning/phases/20-otp-security-policy-core/20-01-PLAN.md
@routes/auth.py
@templates/verify_otp.html
@tests/test_csrf_and_routes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace register hardcoded OTP path with issued challenge references</name>
  <read_first>routes/auth.py, utils/otp_security.py, models/models.py</read_first>
  <files>routes/auth.py</files>
  <behavior>
    - Register creates a fresh OTP challenge and stores only challenge reference in session.
    - Re-issued challenge invalidates previously active challenge for same email/purpose.
    - User feedback no longer leaks OTP values.
  </behavior>
  <action>In routes/auth.py register() flow, replace demo OTP logic with issue_otp_challenge call. Persist session keys pending_registration, pending_otp_challenge_id, pending_verification_email only. Remove any session otp_code usage and remove all hardcoded '123456' fallback or demo flash content. Ensure issue logic marks prior active challenges as superseded with invalidated_at timestamp before storing the new challenge.</action>
  <acceptance_criteria>
    - routes/auth.py no longer contains session.get('otp_code'
    - routes/auth.py no longer contains '123456'
    - routes/auth.py writes session['pending_otp_challenge_id']
    - routes/auth.py writes session['pending_verification_email']
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile routes/auth.py</automated>
  </verify>
  <done>Register flow issues OTP challenge references without hardcoded fallback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce verify lifecycle policy (expiry, attempts, lockout, single-use)</name>
  <read_first>routes/auth.py, utils/otp_security.py, models/models.py, config.py</read_first>
  <files>routes/auth.py</files>
  <behavior>
    - Verify rejects expired, locked, used, or superseded challenges.
    - Wrong attempts increment attempts_used and lock challenge after threshold.
    - Successful verify marks challenge used and cannot be replayed.
  </behavior>
  <action>Refactor verify_otp() to load pending_registration + pending_otp_challenge_id and query OtpChallenge. Call verify_otp_submission helper for policy evaluation. For invalid submissions: increment attempts_used and set locked_until when attempts reach max_attempts. For success: mark challenge status used with used_at, create Registration, clear pending_registration/pending_otp_challenge_id/pending_verification_email from session, and prevent subsequent reuse by checking terminal statuses before any password creation path.</action>
  <acceptance_criteria>
    - routes/auth.py checks challenge expiry before acceptance
    - routes/auth.py updates attempts_used on invalid OTP submissions
    - routes/auth.py sets locked_until when attempts exceed threshold
    - routes/auth.py sets used_at or status used on successful verification
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile routes/auth.py</automated>
  </verify>
  <done>Verify route enforces TTL, lockout, and replay-safe single-use behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Remove OTP disclosure from verify template</name>
  <read_first>templates/verify_otp.html, routes/auth.py</read_first>
  <files>templates/verify_otp.html</files>
  <action>Update verify_otp.html to remove any demo OTP hint text. Keep Vietnamese UX copy and display pending verification email from session pending_verification_email (fallback to pending_registration email if needed), without rendering OTP value anywhere.</action>
  <acceptance_criteria>
    - templates/verify_otp.html does not contain '123456'
    - templates/verify_otp.html renders pending_verification_email context
    - templates/verify_otp.html keeps Vietnamese labels/messages
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_csrf_and_routes.py -k "register_success_redirects_to_otp or verify_otp_no_pending_registration"</automated>
  </verify>
  <done>Verify page no longer discloses OTP demo values and remains functional.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> /register and /verify-otp | Untrusted registration and OTP values enter auth policy checks |
| session -> database challenge | Session challenge reference binds to durable OTP challenge state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-04 | T | verify_otp route state transitions | mitigate | Enforce explicit status transitions (active -> used/locked/expired/superseded) |
| T-20-05 | D | OTP brute force on verify endpoint | mitigate | Increment attempts_used and apply lockout window from config thresholds |
| T-20-06 | R | OTP replay after successful verify | mitigate | Mark challenge used and reject terminal statuses on subsequent submits |
</threat_model>

<verification>
- Validate auth route syntax and run OTP-adjacent smoke tests that should remain stable.
- Confirm hardcoded OTP literals are removed from route and template layer.
</verification>

<success_criteria>

- Register/verify flow uses challenge-based lifecycle controls.
- OTP verification is TTL-aware, lockout-enforced, and replay-safe.
- Verify UI no longer exposes any static OTP value.
</success_criteria>

<output>
After completion, create `.planning/phases/20-otp-security-policy-core/20-02-SUMMARY.md`
</output>
