---
phase: 09-vercel-deployment-verification
plan: 01
type: execute
wave: 1
depends_on: ["08-01"]
files_modified: []
autonomous: true
requirements: [VDEP-01, VDEP-02]
must_haves:
  truths:
    - "All required env vars are set in Vercel project settings (production)"
    - "Vercel build completes in ~15s with no errors"
    - "Homepage, quiz, report, auth pages all return HTTP 200"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel build + routing config"
  key_links:
    - from: "Vercel production"
      to: "NeonDB"
      via: "DATABASE_URL env var → psycopg2-binary → NullPool"
---

<objective>
Configure all required environment variables on Vercel and verify the production
deployment serves all key pages with HTTP 200.

Purpose: Resolve the original 500 errors (SQLite ephemeral + missing env vars).
Output: Live production deployment at https://mindguard-five.vercel.app
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-app-startup-cleanup-data-seeding/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Vercel CLI and configure env vars</name>
  <files>vercel.json</files>
  <action>
    Install Vercel CLI: npm install -g vercel
    Verify auth: vercel whoami
    Add missing env vars via CLI:
      - CLOUDFLARE_SITE_KEY (production)
      - CLOUDFLARE_SECRET_KEY (production)
    DATABASE_URL, SECRET_KEY, OPENROUTER_API_KEY were already set.
  </action>
  <verify>
    <automated>vercel env ls production</automated>
  </verify>
  <done>All 5 required env vars present: DATABASE_URL, SECRET_KEY, CLOUDFLARE_SITE_KEY, CLOUDFLARE_SECRET_KEY, OPENROUTER_API_KEY</done>
</task>

<task type="auto">
  <name>Task 2: Deploy and verify all pages return 200</name>
  <files></files>
  <action>
    Run: vercel --yes --prod
    Verify all pages return 200 via Python requests.
  </action>
  <verify>
    <automated>python -c "import requests; pages = ['/', '/login', '/register', '/quiz', '/scammer/report', '/leaderboard']; [print(requests.get('https://mindguard-five.vercel.app'+p).status_code, p) for p in pages]"</automated>
  </verify>
  <done>All 7 pages return HTTP 200 on https://mindguard-five.vercel.app</done>
</task>

</tasks>

<verification>
- vercel env ls production shows all required vars
- All pages: / /login /register /quiz /scammer/report /leaderboard /chatbot → 200
- vercel ls shows latest deployment as "Ready"
</verification>

<success_criteria>
1. VDEP-01: DATABASE_URL, SECRET_KEY, CLOUDFLARE_SITE_KEY, CLOUDFLARE_SECRET_KEY, OPENROUTER_API_KEY all set
2. VDEP-02: All key pages HTTP 200 on production URL
3. Cold start completes in ≤ 31s (observed: 31s build time)
</success_criteria>
