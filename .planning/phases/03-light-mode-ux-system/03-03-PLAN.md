---
phase: 03-light-mode-ux-system
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - static/css/leaderboard.css
  - static/css/scammer_profile.css
  - templates/leaderboard.html
  - templates/scammer_profile.html
  - tests/ui/test_leaderboard_profile_light.py
  - tests/ui/test_token_coverage.py
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Leaderboard va scammer profile hien thi light mode nhat quan voi auth/report/quiz/profile da migration."
    - "Priority pages deu consume design tokens thay vi hardcoded mau toi, giam drift toan phase."
    - "Phase co bang chung automated cho token coverage tren cac template uu tien."
  artifacts:
    - path: "static/css/leaderboard.css"
      provides: "Tokenized leaderboard surfaces/chips/cards/cta states aligned to light system."
    - path: "static/css/scammer_profile.css"
      provides: "Tokenized profile detail surfaces and status emphasis with readability-first contrast."
    - path: "templates/leaderboard.html"
      provides: "Light-consistent class usage for ranking UI."
    - path: "templates/scammer_profile.html"
      provides: "Light-consistent class usage for profile and evidence blocks."
    - path: "tests/ui/test_token_coverage.py"
      provides: "Automated coverage checks that priority templates rely on tokenized classes/variables."
  key_links:
    - from: "templates/leaderboard.html"
      to: "static/css/leaderboard.css"
      via: "semantic classes for leaderboard cards and badges"
      pattern: "leaderboard|mg-"
    - from: "templates/scammer_profile.html"
      to: "static/css/scammer_profile.css"
      via: "semantic classes for profile sections and metadata rows"
      pattern: "profile|mg-"
    - from: "tests/ui/test_token_coverage.py"
      to: "templates/*.html"
      via: "static assertions for tokenized class usage"
      pattern: "mg-|token"
---

<objective>
Hoan tat light-mode rollout tren leaderboard va scammer profile, sau do dong phase bang token coverage verification cho tat ca trang uu tien.

Purpose: Chot UI-01/UI-02 end-to-end va bao dam he thong co regression guard truoc khi sang Phase 4.
Output: Leaderboard/profile CSS-template migration + coverage tests cho token adoption.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-light-mode-ux-system/3-CONTEXT.md
@.planning/phases/03-light-mode-ux-system/03-RESEARCH.md
@.planning/phases/03-light-mode-ux-system/03-VALIDATION.md
@.planning/phases/03-light-mode-ux-system/03-01-SUMMARY.md
@.planning/phases/03-light-mode-ux-system/03-02-SUMMARY.md
@templates/leaderboard.html
@templates/scammer_profile.html
@static/css/leaderboard.css
@static/css/scammer_profile.css

<interfaces>
From tests/ui contract files:
```python
# test_leaderboard_profile_light.py and test_token_coverage.py
# validate template class usage and token references
```

From priority templates:
```html
<!-- Keep existing Flask/Jinja data bindings; change only presentation classes/styles -->
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate leaderboard and scammer profile pages to light-token visual system</name>
  <read_first>
    - .planning/phases/03-light-mode-ux-system/03-RESEARCH.md
    - static/css/leaderboard.css
    - static/css/scammer_profile.css
    - templates/leaderboard.html
    - templates/scammer_profile.html
  </read_first>
  <files>static/css/leaderboard.css, static/css/scammer_profile.css, templates/leaderboard.html, templates/scammer_profile.html</files>
  <action>Refactor leaderboard/scammer-profile styles va markup classes de consume token layer, loai bo dark utility drift va dam bao readability/spacing dong bo voi phases truoc. Giu semantics warning/danger, giu wiring route va anti-spam/privacy context hien co, khong chen leaderboard integrity logic (deferred sang Phase 5).</action>
  <verify>
    <automated>python -m unittest tests/ui/test_leaderboard_profile_light.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Leaderboard va scammer profile dat light-mode consistency.
    - Khong co visual regression obvious voi privacy-masked fields va badges hien co.
    - Test leaderboard/profile light pass.
  </acceptance_criteria>
  <done>Hai page con lai trong scope UI-01 da rollout xong tren light system.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce phase-wide token coverage guards for priority templates</name>
  <read_first>
    - tests/ui/test_token_coverage.py
    - tests/ui/test_light_tokens.py
    - templates/base.html
    - templates/report_scammer.html
    - templates/quiz.html
    - templates/leaderboard.html
    - templates/scammer_profile.html
  </read_first>
  <files>tests/ui/test_token_coverage.py</files>
  <action>Cap nhat/hoan thien token coverage tests de check cac template priority (auth, quiz, report, profile, leaderboard, scammer_profile) da dung tokenized classes/vars va khong con dark utility bi cam trong scope. Test phai phu hop validation map va cho output de debug khi regression xay ra.</action>
  <verify>
    <automated>python -m unittest tests/ui/test_token_coverage.py -v && python -m unittest discover -s tests/ui -p "test_*.py" -v</automated>
  </verify>
  <acceptance_criteria>
    - Token coverage test bao ve duoc toan bo priority pages.
    - Full tests/ui suite xanh sau migration.
    - Neu co dark utility con sot, test fail voi thong bao ro template/selectors vi pham.
  </acceptance_criteria>
  <done>Phase co regression guard ro rang cho UI-01/UI-02 truoc khi chuyen phase tiep theo.</done>
</task>

</tasks>

<verification>
- python -m unittest tests/ui/test_leaderboard_profile_light.py -v
- python -m unittest tests/ui/test_token_coverage.py -v
- python -m unittest discover -s tests/ui -p "test_*.py" -v
</verification>

<success_criteria>
- Cac trang auth, quiz, report, profile, leaderboard deu dong bo light mode theo token system.
- Bo token contract + coverage tests ngan drift cho cac thay doi phase sau.
- Phase 03 hoan tat day du UI-01/UI-02/UI-03 readiness cho verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/03-light-mode-ux-system/03-03-SUMMARY.md`
</output>
