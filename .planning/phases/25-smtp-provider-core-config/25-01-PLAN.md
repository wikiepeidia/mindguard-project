---
phase: 25-smtp-provider-core-config
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - services/otp_email_delivery.py
  - documents/docs/technical/ARCHITECTURE.md
autonomous: true
requirements:
  - SMTPP-01
  - SMTPP-02
  - SMTPP-03
  - SMTPO-01
must_haves:
  truths:
    - OTP delivery can use a generic SMTP provider when `EMAIL_PROVIDER=smtp`.
    - SMTP config is read from environment-driven settings instead of domain-verified Resend-only settings.
    - Bad SMTP sender or credentials fail closed through the normalized delivery contract.
  artifacts:
    - path: config.py
      provides: SMTP provider config contract and readiness helpers
    - path: services/otp_email_delivery.py
      provides: SMTP delivery adapter with normalized outcomes
    - path: documents/docs/technical/ARCHITECTURE.md
      provides: Updated architecture description for OTP delivery transport
  key_links:
    - from: config.py
      to: services/otp_email_delivery.py
      via: EMAIL_PROVIDER and SMTP_* settings
      pattern: EMAIL_PROVIDER|SMTP_HOST|SMTP_PORT|SMTP_USERNAME|SMTP_PASSWORD|SMTP_FROM_EMAIL
    - from: services/otp_email_delivery.py
      to: extensions.py
      via: Flask-Mail Message/send usage
      pattern: Message|mail\.send
---

<objective>
Build the SMTP provider core so OTP delivery can run on Vercel without Resend domain verification.

Purpose: Unblock the mail transport layer while preserving the auth route contract and fail-closed behavior already enforced by OTP phases 20 to 24.
Output: SMTP-ready config helpers, a Flask-Mail-backed SMTP adapter, and updated architecture documentation.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-smtp-provider-core-config/25-CONTEXT.md
@.planning/phases/25-smtp-provider-core-config/25-RESEARCH.md
@config.py
@services/otp_email_delivery.py
@extensions.py
@documents/docs/technical/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SMTP config contract and readiness helpers</name>
  <files>config.py</files>
  <action>Per D-03 and D-05, extend `Config` with provider-neutral SMTP keys (`SMTP_HOST`, `SMTP_PORT`, `SMTP_USERNAME`, `SMTP_PASSWORD`, `SMTP_USE_TLS`, `SMTP_USE_SSL`, `SMTP_FROM_EMAIL`) sourced from environment first and optional local JSON second. Derive compatible Flask-Mail `MAIL_*` settings from that contract, and update `otp_email_missing_config()` / `otp_email_config_ready()` so `smtp` and `resend_api` are both validated without hardcoded secrets.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_email_delivery.py -q</automated>
  </verify>
  <done>`Config` exposes a complete SMTP contract and can distinguish missing SMTP config from a supported provider that is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Add SMTP adapter and document the provider boundary</name>
  <files>services/otp_email_delivery.py, documents/docs/technical/ARCHITECTURE.md</files>
  <action>Per D-01, D-02, and D-04, add an SMTP branch in `send_otp_email()` that reuses Flask-Mail message delivery and normalizes SMTP success/failure categories without changing auth-route call sites. Keep the Resend branch intact. Update the architecture doc so OTP delivery is described as a provider boundary selected by config rather than a single fixed transport.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_email_delivery.py -q</automated>
  </verify>
  <done>The OTP delivery service supports `smtp` through Flask-Mail, preserves normalized outcomes, and the technical architecture doc matches the new provider boundary.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime config -> SMTP transport | Invalid sender/auth/TLS settings can silently break OTP delivery if not validated before send |
| OTP delivery service -> auth flow | Incorrect provider error normalization can cause auth state corruption or false success |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | T | `config.py` SMTP contract | mitigate | Validate required SMTP settings and reject conflicting TLS/SSL combinations before send |
| T-25-02 | E | `services/otp_email_delivery.py` SMTP branch | mitigate | Preserve the normalized fail-closed result contract so auth routes do not treat SMTP exceptions as success |
| T-25-03 | I | documentation drift | mitigate | Update the architecture doc as part of the same plan so operators know the new provider boundary |
</threat_model>

<verification>
Run `python -m pytest tests/test_otp_email_delivery.py -q` and confirm both provider validation and SMTP outcome tests pass.
</verification>

<success_criteria>
MindGuard can be configured for SMTP-backed OTP delivery without changing auth routes, and misconfiguration is surfaced through the normalized provider result contract.
</success_criteria>

<output>
After completion, create `.planning/phases/25-smtp-provider-core-config/25-01-SUMMARY.md`
</output>
