---
phase: 01-privacy-data-governance-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - models/models.py
  - database/migrate_sensitive_access_log.py
  - services/sensitive_access_log.py
  - routes/admin.py
  - templates/admin_sensitive_access_logs.html
  - tests/privacy/test_sensitive_access_audit.py
  - tests/privacy/test_admin_export_policy.py
autonomous: true
requirements:
  - PRIV-03
  - PRIV-02
must_haves:
  truths:
    - "Moi lan admin truy cap full-data (view/export/update) deu tao audit event co actor, timestamp, action, object, request metadata."
    - "Export admin mac dinh masked; chi cho full-data khi co reason hop le."
    - "Admin co trang xem nhat ky truy cap voi filter theo thoi gian, actor va action."
  artifacts:
    - path: "models/models.py"
      provides: "Model luu sensitive access logs cho kiem toan PRIV-03."
    - path: "database/migrate_sensitive_access_log.py"
      provides: "Migration script idempotent tao bang/index audit log."
    - path: "services/sensitive_access_log.py"
      provides: "Service ghi/loc/retention audit logs dung chung."
    - path: "routes/admin.py"
      provides: "Hook logging cho full-data access va enforce reason khi export full."
    - path: "templates/admin_sensitive_access_logs.html"
      provides: "UI bang/filter de admin kiem toan truy cap."
  key_links:
    - from: "routes/admin.py"
      to: "services/sensitive_access_log.py"
      via: "log_sensitive_access(...) tai cac action view/export/update"
      pattern: 'log_sensitive_access\('
    - from: "services/sensitive_access_log.py"
      to: "models/models.py"
      via: "db.session add/commit SensitiveAccessLog"
      pattern: "SensitiveAccessLog"
    - from: "routes/admin.py"
      to: "templates/admin_sensitive_access_logs.html"
      via: "render_template cho trang nhat ky + filters"
      pattern: "admin_sensitive_access_logs"
---

<objective>
Hoan tat governance cho du lieu nhay cam bang audit trail co the truy vet va policy export admin an toan theo dung locked decisions.

Purpose: Dat PRIV-03 va cung co PRIV-02 o nhanh admin (masked by default, full-data co ly do va co log).
Output: Audit log schema/service, admin route enforcement, trang nhat ky co bo loc, va bo test governance.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-privacy-data-governance-foundation/1-CONTEXT.md
@.planning/phases/01-privacy-data-governance-foundation/01-RESEARCH.md
@models/models.py
@routes/admin.py
@database/migrate_profile_v2.py

<interfaces>
From models/models.py:
```python
class Registration(db.Model):
    id = db.Column(db.Integer, primary_key=True)
    email = db.Column(db.String(150), nullable=False, unique=True)
    is_admin = db.Column(db.Boolean, default=False)
```

From routes/admin.py:

```python
@admin_bp.route("/scammer-reports")
@admin_required
def scammer_reports():
    ...

@admin_bp.route("/export-dataset")
@admin_required
def export_dataset():
    ...
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Them schema va service cho sensitive access audit log</name>
  <read_first>
    - models/models.py
    - database/migrate_profile_v2.py
    - .planning/phases/01-privacy-data-governance-foundation/01-RESEARCH.md
  </read_first>
  <files>models/models.py, database/migrate_sensitive_access_log.py, services/sensitive_access_log.py, tests/privacy/test_sensitive_access_audit.py</files>
  <behavior>
    - Test 1: Log row luu du actor/action/object/reason/ip/user_agent/created_at khi goi service.
    - Test 2: Query filter theo actor/action/time tra ve dung tap ban ghi.
    - Test 3: Retention remove logs > 90 ngay.
  </behavior>
  <action>Them model `SensitiveAccessLog` vao `models/models.py`; tao migration script thu cong idempotent trong `database/`; tao service trong `services/sensitive_access_log.py` cho ghi log, loc log va retention cleanup 90 ngay. Bao dam import `db` tu `extensions` theo convention du an.</action>
  <verify>
    <automated>python -m unittest tests/privacy/test_sensitive_access_audit.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Bang audit log tao duoc boi migration script khi chay nhieu lan.
    - Service ghi du metadata bat buoc tu request context.
    - Unit/integration test cho service pass.
  </acceptance_criteria>
  <done>Nen tang luu vet truy cap du lieu nhay cam san sang cho admin flows.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce admin full-data policy va ghi log o cac action nhay cam</name>
  <read_first>
    - routes/admin.py
    - services/sensitive_access_log.py
    - .planning/phases/01-privacy-data-governance-foundation/1-CONTEXT.md
  </read_first>
  <files>routes/admin.py, tests/privacy/test_admin_export_policy.py</files>
  <action>Cap nhat cac admin action `view/export/update` de goi `log_sensitive_access(...)` moi khi truy cap full-data. Voi export dataset: mac dinh masked output; chi cho full-data khi co tham so explicit va `reason` hop le (non-empty), neu thieu reason thi tu choi va thong bao loi ro rang.</action>
  <verify>
    <automated>python -m unittest tests/privacy/test_admin_export_policy.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Export khong co reason khong the tra full-data.
    - Export masked van hoat dong theo mac dinh.
    - Moi action full-data co tao audit row tuong ung.
  </acceptance_criteria>
  <done>Policy admin "masked by default + reasoned full access + mandatory logging" duoc enforce nhat quan.</done>
</task>

<task type="auto">
  <name>Task 3: Xay trang admin audit log co bo loc actor/time/action</name>
  <read_first>
    - routes/admin.py
    - templates/admin_dashboard.html
    - templates/admin_scammer_reports.html
  </read_first>
  <files>routes/admin.py, templates/admin_sensitive_access_logs.html</files>
  <action>Them route admin cho trang nhat ky truy cap du lieu nhay cam va render bang log voi filter theo khoang thoi gian, actor, action. Hien thi canh bao tan suat cao theo actor/IP dua tren nguong cau hinh de ho tro giam sat ban dau trong Phase 1.</action>
  <verify>
    <automated>python -m unittest tests/privacy/test_sensitive_access_audit.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Admin dang nhap xem duoc bang nhat ky co du cot bat buoc.
    - Filter actor/time/action tra ve ket qua dung.
    - Hien canh bao khi vuot nguong truy cap full-data.
  </acceptance_criteria>
  <done>Admin co kha nang kiem toan va giam sat truy cap full-data theo PRIV-03.</done>
</task>

</tasks>

<verification>
- python -m unittest tests/privacy/test_sensitive_access_audit.py tests/privacy/test_admin_export_policy.py -v
- python -m unittest discover -s tests/privacy -p "test_*.py" -v
</verification>

<success_criteria>

- Ton tai audit log day du metadata cho moi full-data admin action trong scope phase.
- Export full-data bi rang buoc reason, va masked la default output.
- Admin UI co filter log va thong tin canh bao de audit truy cap.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-data-governance-foundation/01-02-SUMMARY.md`
</output>
