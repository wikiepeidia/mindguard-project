---
phase: 04-quiz-one-question-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/quiz.py
  - tests/quizflow/__init__.py
  - tests/quizflow/test_quiz_step_flow.py
  - tests/quizflow/test_state_resume.py
autonomous: true
requirements:
  - QUIZ-01
  - QUIZ-03
must_haves:
  truths:
    - "User always sees exactly one question per quiz step route render."
    - "User can move step-by-step without losing already selected answers inside the same valid session."
    - "Refresh on current step does not create duplicated score writes or randomize the in-progress question set."
  artifacts:
    - path: "routes/quiz.py"
      provides: "Session-backed quiz attempt state machine with per-step PRG navigation"
    - path: "tests/quizflow/test_quiz_step_flow.py"
      provides: "Automated checks for one-question-per-page behavior and step navigation"
    - path: "tests/quizflow/test_state_resume.py"
      provides: "Automated checks for refresh/back stability in a valid session"
  key_links:
    - from: "routes/quiz.py"
      to: "templates/quiz.html"
      via: "render context exposes single current question + step metadata"
      pattern: "current_question|current_index|total_questions"
    - from: "tests/quizflow/test_quiz_step_flow.py"
      to: "routes/quiz.py"
      via: "Flask test client requests /quiz and /quiz/step transitions"
      pattern: "quiz step client requests"
---

<objective>
Implement a server-trusted one-question quiz attempt flow with stable session state and deterministic step transitions.
Purpose: Deliver QUIZ-01 baseline safely before UI polish by making route behavior the source of truth.
Output: Refactored `routes/quiz.py` step lifecycle and passing backend-focused flow/persistence tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-quiz-one-question-flow/4-CONTEXT.md
@.planning/phases/04-quiz-one-question-flow/04-RESEARCH.md
@.planning/phases/04-quiz-one-question-flow/04-VALIDATION.md
@routes/quiz.py
@templates/quiz.html

<interfaces>
From routes/quiz.py (current contract to preserve):
```python
@quiz_bp.route("/quiz", methods=["GET", "POST"])
@login_required
def quiz(): ...

@quiz_bp.route("/quiz/result")
@login_required
def quiz_result(): ...

@quiz_bp.route("/certificate")
@login_required
def certificate(): ...
```

From models/models.py usage in current route:
```python
result = QuizResult(name=name, email=email, score=score, max_score=max_score)
db.session.add(result)
db.session.commit()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce one-question route contract tests and session resume tests</name>
  <read_first>
    - .planning/phases/04-quiz-one-question-flow/04-RESEARCH.md
    - .planning/phases/04-quiz-one-question-flow/04-VALIDATION.md
    - routes/quiz.py
  </read_first>
  <files>tests/quizflow/__init__.py, tests/quizflow/test_quiz_step_flow.py, tests/quizflow/test_state_resume.py</files>
  <behavior>
    - Test 1: GET `/quiz` renders exactly one active question in context and exposes total question count.
    - Test 2: POST step answer updates session answer map then redirects to next step (PRG).
    - Test 3: Reload current step keeps same attempt/question order and persisted answer values.
  </behavior>
  <action>Create deterministic Flask test-client coverage for one-question flow and in-session resume. Add lightweight helper setup inside tests package for authenticated session bootstrap if needed. Keep tests independent from external AI APIs by stubbing dynamic question generation path.</action>
  <verify>
    <automated>python -m unittest tests.quizflow.test_quiz_step_flow tests.quizflow.test_state_resume -v</automated>
  </verify>
  <acceptance_criteria>
    - New tests run in under 60 seconds and do not call network services.
    - Tests clearly fail when route returns multiple questions or loses session answers.
  </acceptance_criteria>
  <done>Backend contract tests for QUIZ-01 and QUIZ-03 exist and pass against implemented flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor quiz route to session-backed step lifecycle with PRG transitions</name>
  <read_first>
    - routes/quiz.py
    - utils/quiz_data.py
    - tests/quizflow/test_quiz_step_flow.py
    - tests/quizflow/test_state_resume.py
  </read_first>
  <files>routes/quiz.py</files>
  <behavior>
    - Test 1: Attempt initialization freezes question order once per attempt.
    - Test 2: Step submit mutates only answer map/current index, then redirects.
    - Test 3: Refresh/back in valid session does not duplicate DB writes.
  </behavior>
  <action>Implement attempt envelope in Flask session (`quiz_attempt`) with keys for questions, answers, current_index, and total_questions. Split handling into start/view/step-submit/finalize branches while preserving existing `quiz_result` and `certificate` route contracts (`last_quiz_score`, `max_quiz_score`, `certificate_code`, DB `QuizResult` write). Use POST-Redirect-GET for all step submissions and final submit.</action>
  <verify>
    <automated>python -m unittest tests.quizflow.test_quiz_step_flow tests.quizflow.test_state_resume -v</automated>
  </verify>
  <acceptance_criteria>
    - `/quiz` no longer renders full-question batch on one page.
    - Step submits are idempotent on refresh because of PRG redirects.
    - Existing result/certificate routes still resolve correctly after completion.
  </acceptance_criteria>
  <done>Server-side one-question state machine is in place and validated by passing flow/resume tests.</done>
</task>

</tasks>

<verification>
- python -m unittest tests.quizflow.test_quiz_step_flow tests.quizflow.test_state_resume -v
</verification>

<success_criteria>
- QUIZ-01 baseline behavior is implemented server-side (one question per step).
- QUIZ-03 baseline persistence is stable for refresh/back in valid session.
- No regression in score persistence and certificate redirect contract.
</success_criteria>

<output>
After completion, create `.planning/phases/04-quiz-one-question-flow/04-01-SUMMARY.md`
</output>
