---
phase: 12-ai-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - utils/chatbot.py
autonomous: false
requirements:
  - AISF-01

must_haves:
  truths:
    - "Chatbot OpenRouter call hoàn thành trong vòng 8 giây — Vercel function không bị kill do timeout"
    - "ADMIN_PASSWORD và REPORT_ENCRYPTION_KEY được set trên Vercel production"
    - "App sau deploy vẫn trả về 200 trên các trang chính"
  artifacts:
    - path: "utils/chatbot.py"
      provides: "OpenRouter timeout giảm từ 10s xuống 8s"
      contains: "timeout=8"
  key_links:
    - from: "utils/chatbot.py"
      to: "OpenRouter API"
      via: "urllib.request.urlopen(req, timeout=8)"
      pattern: "timeout=8"
---

<objective>
Hoàn tất Phase 12 AI Safety bằng cách fix timeout chatbot (AISF-01) và push env vars còn thiếu lên Vercel.

Purpose: Vercel function timeout là 10s — chatbot đang dùng timeout=10 sẽ race với kill limit. Giảm xuống 8s tạo buffer 2s. Đồng thời hoàn tất INFRA-02 carry-over từ Phase 10 (code đã đúng, env vars chưa được set trên Vercel).

Output: `utils/chatbot.py` với timeout=8, Vercel production có đủ env vars, redeploy thành công.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-infrastructure-security-hardening/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fix chatbot timeout 10s → 8s (AISF-01)</name>
  <files>utils/chatbot.py</files>
  <action>
    Trong `utils/chatbot.py` tại hàm `query_ai_model`, dòng:

    ```python
    with urllib.request.urlopen(req, timeout=10) as response:
    ```

    Đổi thành:

    ```python
    with urllib.request.urlopen(req, timeout=8) as response:
    ```

    Chỉ thay đổi con số timeout. Không sửa gì khác trong file.
    
    Lý do: Vercel hobby tier giới hạn function execution 10s. Timeout=10 sẽ race với kill signal. Buffer 2s để response kịp serialize và trả về trước khi bị kill.
  </action>
  <verify>
    grep -n "timeout=8" utils/chatbot.py
    # Phải thấy dòng chứa timeout=8
    # Đảm bảo KHÔNG còn timeout=10:
    grep -n "timeout=10" utils/chatbot.py
    # Không được có kết quả
  </verify>
  <done>utils/chatbot.py chứa timeout=8, không còn timeout=10</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set Vercel env vars via CLI</name>
  <what-built>Task 1 đã fix timeout trong code. Bước này set credentials trên Vercel production — agent không biết giá trị thực của secrets.</what-built>
  <how-to-verify>
    Chạy các lệnh sau trong terminal (thay giá trị thực của bạn vào):

    ```bash
    # Set ADMIN_PASSWORD (mật khẩu admin của bạn)
    vercel env add ADMIN_PASSWORD production

    # Set REPORT_ENCRYPTION_KEY (encryption key của bạn)
    vercel env add REPORT_ENCRYPTION_KEY production
    ```

    Sau khi chạy xong, xác nhận bằng:

    ```bash
    vercel env ls production
    ```

    Kiểm tra cả hai biến xuất hiện trong danh sách (giá trị sẽ được ẩn).

    **Lưu ý:** Nếu biến đã tồn tại, dùng `vercel env rm ADMIN_PASSWORD production` trước, rồi `vercel env add` lại.
  </how-to-verify>
  <resume-signal>Gõ "done" sau khi đã set cả hai env vars và xác nhận chúng xuất hiện trong `vercel env ls production`</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Deploy lên Vercel production và verify</name>
  <files></files>
  <action>
    1. Commit thay đổi timeout:
    ```bash
    git add utils/chatbot.py
    git commit -m "fix(chatbot): reduce OpenRouter timeout 10s → 8s to avoid Vercel kill (AISF-01)

    Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"
    ```

    2. Deploy lên Vercel production:
    ```bash
    vercel --prod --yes
    ```

    3. Sau khi deploy xong, lấy production URL từ output và chạy health checks:
    ```bash
    # Thay YOUR_VERCEL_URL bằng URL thực từ output deploy
    curl -s -o /dev/null -w "%{http_code}" https://YOUR_VERCEL_URL/
    curl -s -o /dev/null -w "%{http_code}" https://YOUR_VERCEL_URL/auth/login
    curl -s -o /dev/null -w "%{http_code}" https://YOUR_VERCEL_URL/quiz/
    ```

    Tất cả phải trả về 200 hoặc 302 (redirect là OK cho các trang login-protected).

    4. Verify chatbot API không bị timeout:
    ```bash
    curl -s -X POST https://YOUR_VERCEL_URL/chatbot/api \
      -H "Content-Type: application/json" \
      -d '{"message":"lừa đảo ngân hàng là gì"}' \
      -w "\nHTTP %{http_code} in %{time_total}s\n"
    ```
    Thời gian phải < 8 giây, HTTP 200.
  </action>
  <verify>
    vercel --prod --yes
    # Deploy phải thành công (exit code 0)
    # URL homepage trả về 200 hoặc 302
  </verify>
  <done>
    - Commit với message fix(chatbot) tồn tại trong git log
    - vercel --prod deploy thành công không có build errors
    - Homepage trả về 200/302
    - Chatbot /api endpoint không bị timeout
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → /chatbot/api | Untrusted message input crosses into AI call |
| Agent → Vercel CLI | Credentials never passed through agent — human sets them directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Denial of Service | utils/chatbot.py urlopen | mitigate | timeout=8 prevents function from hanging past Vercel kill limit |
| T-12-02 | Information Disclosure | config.py ADMIN_PASSWORD | mitigate | env var only — empty string fallback causes auth failure, not exposure |
| T-12-03 | Spoofing | /chatbot/api no auth | accept | Widget endpoint intentionally unauthenticated, rate-limited at 10/min;2/s |
</threat_model>

<verification>
Sau khi hoàn tất tất cả tasks:

1. `grep -n "timeout" utils/chatbot.py` — chỉ thấy `timeout=8`
2. `vercel env ls production` — thấy `ADMIN_PASSWORD` và `REPORT_ENCRYPTION_KEY`
3. `vercel --prod` deploy log — no errors
4. `curl https://YOUR_URL/` — HTTP 200 hoặc 302
</verification>

<success_criteria>
Phase 12 hoàn tất khi:

- utils/chatbot.py chứa `timeout=8` (không còn `timeout=10`)
- ADMIN_PASSWORD và REPORT_ENCRYPTION_KEY xuất hiện trong `vercel env ls production`
- Vercel deployment thành công sau khi set env vars
- AISF-01 ✓ — chatbot timeout không vượt quá 8s
</success_criteria>

<output>
Sau khi hoàn tất, tạo `.planning/phases/12-ai-safety/12-01-SUMMARY.md` với:
- Thay đổi đã thực hiện
- Kết quả verify (output của curl, grep)
- Trạng thái AISF-01: complete
</output>
