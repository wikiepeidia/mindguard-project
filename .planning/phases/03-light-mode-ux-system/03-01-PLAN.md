---
phase: 03-light-mode-ux-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - static/css/tokens.css
  - static/css/style.css
  - static/css/base.css
  - templates/base.html
  - templates/login.html
  - templates/register.html
  - templates/profile.html
  - tests/ui/test_light_tokens.py
  - tests/ui/test_base_light_mode.py
autonomous: true
requirements:
  - UI-01
  - UI-02
must_haves:
  truths:
    - "Auth va profile surfaces hien thi light mode nhat quan, khong con dark utility chi dao giao dien."
    - "Global shell (navbar/footer/flash/chat shell) dung semantic tokens thay cho mau hardcode dark-first."
    - "Design token layer la nguon su that duy nhat cho mau, text, border, spacing co ban tren cac trang uu tien."
  artifacts:
    - path: "static/css/tokens.css"
      provides: "Semantic light tokens (canvas/surface/text/border/accent/status/focus/spacing/radius/shadow)."
    - path: "static/css/style.css"
      provides: "Global components consume tokens; bo goc dark-first declarations.
"
    - path: "static/css/base.css"
      provides: "Shared shell/chat/nav/footer aligned to light tokens and mobile-first defaults."
    - path: "templates/base.html"
      provides: "Dark utility replacement map in shared layout and flash/footer blocks."
    - path: "tests/ui/test_light_tokens.py"
      provides: "Automated token contract checks for required variables."
  key_links:
    - from: "templates/base.html"
      to: "static/css/tokens.css"
      via: "link include order and semantic class usage"
      pattern: "tokens.css|mg-"
    - from: "static/css/style.css"
      to: "tests/ui/test_light_tokens.py"
      via: "required variable declarations"
      pattern: "--mg-"
---

<objective>
Tao foundation light-mode token system va migration global shell/auth/profile de dat tinh dong bo co cau truc truoc khi chuyen sang page rollout.

Purpose: Dat nen vung cho UI-01/UI-02, giam style drift va cho phep rollout tung wave an toan.
Output: Token layer + global CSS refactor + base/auth/profile template migration + foundation UI tests xanh.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-light-mode-ux-system/3-CONTEXT.md
@.planning/phases/03-light-mode-ux-system/03-RESEARCH.md
@.planning/phases/03-light-mode-ux-system/03-VALIDATION.md
@templates/base.html
@templates/login.html
@templates/register.html
@templates/profile.html
@static/css/style.css
@static/css/base.css

<interfaces>
From templates/base.html:
```html
<link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
<link rel="stylesheet" href="{{ url_for('static', filename='css/base.css') }}">
```

From static/css/style.css:
```css
:root { ... }
body { ... }
.navbar { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define light-mode token contracts and align global CSS owners</name>
  <read_first>
    - .planning/phases/03-light-mode-ux-system/3-CONTEXT.md
    - .planning/phases/03-light-mode-ux-system/03-RESEARCH.md
    - static/css/style.css
    - static/css/base.css
  </read_first>
  <files>static/css/tokens.css, static/css/style.css, static/css/base.css</files>
  <action>Tao file `tokens.css` chua semantic token map theo locked decisions: canvas/surface light, accent cyan, warning amber, danger red, contrast huong WCAG AA. Cap nhat `style.css` va `base.css` de consume token thay vi hardcoded dark variables; de-duplicate selectors trung lap va phan quyen owner ro (style.css: global component styles, base.css: shell/chatbot behavior). Khong them dark-mode feature moi trong phase nay.</action>
  <verify>
    <automated>python -m unittest tests/ui/test_light_tokens.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `tokens.css` ton tai va export day du nhom token mau/chu/border/spacing/radius/shadow/focus.
    - `style.css` + `base.css` khong con dinh nghia dark-first body/background la source of truth.
    - Test token contract pass.
  </acceptance_criteria>
  <done>Global token foundation san sang cho rollout page-level.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate shared layout and auth/profile templates to semantic light classes</name>
  <read_first>
    - templates/base.html
    - templates/login.html
    - templates/register.html
    - templates/profile.html
    - .planning/phases/03-light-mode-ux-system/03-RESEARCH.md
  </read_first>
  <files>templates/base.html, templates/login.html, templates/register.html, templates/profile.html</files>
  <action>Thay cac dark utility (`bg-dark`, `text-white`, `btn-close-white`, inline dark style) bang semantic classes/token-friendly Bootstrap classes trong base/auth/profile surfaces. Dong bo turnstile theme sang light cho login/register theo guideline phase. Giu nguyen logic Flask/Jinja va route wiring, chi thay doi presentation layer.
</action>
  <verify>
    <automated>python -m unittest tests/ui/test_base_light_mode.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Base shell + auth/profile hien light mode thong nhat va doc de dang.
    - Khong con hardcoded dark class o cac block chinh da scope.
    - Test base light-mode pass.
  </acceptance_criteria>
  <done>UI-01/UI-02 dat baseline cho base + auth + profile.</done>
</task>

</tasks>

<verification>
- python -m unittest tests/ui/test_light_tokens.py -v
- python -m unittest tests/ui/test_base_light_mode.py -v
</verification>

<success_criteria>
- Global design tokens tro thanh contract chung cho phase rollout.
- Base/auth/profile surfaces hien light mode dong bo va contrast dat muc de doc.
- Plan 02 co the consume token layer ma khong can bo sung architecture moi.
</success_criteria>

<output>
After completion, create `.planning/phases/03-light-mode-ux-system/03-01-SUMMARY.md`
</output>
