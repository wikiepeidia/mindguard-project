---
phase: 26-auth-flow-smtp-cutover
plan: 02
type: execute
wave: 2
depends_on:
  - 26-01
files_modified:
  - TODO.mD
autonomous: true
requirements:
  - SMTPO-02
must_haves:
  truths:
    - Operators have a concrete free SMTP setup path for this repo.
    - The repo documents exact env vars for Gmail App Password on Vercel.
    - Failure categories are explained so operators know whether to fix config or retry later.
  artifacts:
    - path: TODO.mD
      provides: Free SMTP setup checklist and readiness matrix for Phase 26
  key_links:
    - from: TODO.mD
      to: config.py
      via: documented EMAIL_PROVIDER and SMTP_* env vars
      pattern: EMAIL_PROVIDER|SMTP_HOST|SMTP_PORT|SMTP_USERNAME|SMTP_PASSWORD|SMTP_FROM_EMAIL
---

<objective>
Add the operator-facing free SMTP setup and readiness checklist requested by the user.

Purpose: Make the Phase 26 cutover actionable without paid infrastructure by documenting the Gmail App Password path, the exact env contract, and what each failure category means.
Output: A concrete free SMTP setup runbook in `TODO.mD`.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-auth-flow-smtp-cutover/26-CONTEXT.md
@config.py
@services/otp_email_delivery.py
@TODO.mD
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the free Gmail App Password setup path</name>
  <files>TODO.mD</files>
  <action>Per D-02, turn `TODO.mD` into a usable operator runbook for the free Gmail App Password path. Include the exact Vercel env vars, a local `.env/SMTP.json` example, and the rule that the Gmail sender should match `SMTP_FROM_EMAIL`.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -q</automated>
  </verify>
  <done>The repo contains a copy-pasteable Gmail SMTP setup guide with the exact env contract used by the app.</done>
</task>

<task type="auto">
  <name>Task 2: Document failure-category triage for operators</name>
  <files>TODO.mD</files>
  <action>Per D-03, add a short diagnostics section that explains `misconfigured`, `provider_rejected`, `timeout`, and `network_error` in operational terms, plus a checklist for Phase 26 cutover readiness. Keep the tone direct and practical.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -q</automated>
  </verify>
  <done>`TODO.mD` tells the operator whether to fix config or retry later based on the same failure categories emitted by the app.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator setup -> production runtime | Wrong env setup can silently break OTP delivery if the runbook is vague or incomplete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04 | T | `TODO.mD` setup steps | mitigate | Document exact env keys, Gmail App Password prerequisites, and redeploy requirements |
| T-26-05 | I | failure triage guidance | mitigate | Explain each delivery category so operators know whether the problem is config or transient runtime |
</threat_model>

<verification>
Run `python -m pytest tests/test_otp_auth_integration.py -q` after documentation changes to confirm the cutover code and route diagnostics remain green.
</verification>

<success_criteria>
The repo contains a free SMTP setup path and a practical failure-triage checklist aligned with the app’s current SMTP behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/26-auth-flow-smtp-cutover/26-02-SUMMARY.md`
</output>
