---
phase: 23-otp-abuse-guardrails
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - tests/antispam/test_otp_guardrails.py
  - tests/test_otp_auth_integration.py
  - .planning/phases/23-otp-abuse-guardrails/23-VALIDATION.md
autonomous: true
requirements: [OTPREL-01, OTPREL-02]
must_haves:
  truths:
    - Automated tests prove route-level limiter blocking on verify and resend.
    - Automated tests prove OTP abuse telemetry activates cooldown and synchronizes the challenge lock.
    - Validation evidence maps OTPREL requirements to exact runnable commands.
  artifacts:
    - path: tests/antispam/test_otp_guardrails.py
      provides: Focused OTP abuse telemetry and cooldown-sync unit tests
    - path: tests/test_otp_auth_integration.py
      provides: OTP verify/resend rate-limit and low-friction happy-path route coverage
    - path: .planning/phases/23-otp-abuse-guardrails/23-VALIDATION.md
      provides: Requirement-to-command validation map for Phase 23
  key_links:
    - from: tests/antispam/test_otp_guardrails.py
      to: services/otp_abuse_guard.py
      via: direct service-level unit assertions
      pattern: otp_abuse_guard
    - from: tests/test_otp_auth_integration.py
      to: routes/auth.py
      via: Flask client POST requests to verify and resend routes
      pattern: /verify-otp
    - from: .planning/phases/23-otp-abuse-guardrails/23-VALIDATION.md
      to: tests/antispam/test_otp_guardrails.py
      via: exact pytest commands used during execution
      pattern: python -m pytest
---

<objective>
Lock Phase 23 with deterministic anti-abuse tests and a validation map that is ready to become execution evidence.

Purpose: Ensure the new OTP guardrails stay reliable and do not regress legitimate verify flows.
Output: Service-level OTP abuse tests, route-level limiter tests, and updated validation evidence.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/23-otp-abuse-guardrails/23-CONTEXT.md
@.planning/phases/23-otp-abuse-guardrails/23-RESEARCH.md
@.planning/phases/23-otp-abuse-guardrails/23-VALIDATION.md
@config.py
@services/otp_abuse_guard.py
@routes/auth.py
@tests/test_otp_auth_integration.py
@tests/antispam/test_decision_service.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OTP abuse telemetry and cooldown-sync unit coverage</name>
  <files>tests/antispam/test_otp_guardrails.py</files>
  <behavior>
    - Repeated OTP abuse events trigger persisted anti-spam cooldown for the OTP actor.
    - OTP cooldown lookup returns only active cooldown state.
    - Challenge lock synchronization keeps the later cooldown instead of shortening an existing lock.
  </behavior>
  <action>Create `tests/antispam/test_otp_guardrails.py` using the existing anti-spam test pattern with an in-memory SQLite app. Add focused unit cases for the new OTP abuse helper/service: repeated OTP abuse events for the same pending email trigger cooldown in `AntiSpamActorState`; active cooldown can be read back for the same OTP actor; and syncing cooldown onto an `OtpChallenge` preserves the later of the current challenge lock and anti-spam cooldown. Keep tests deterministic and avoid any network calls.</action>
  <verify>
    <automated>python -m pytest tests/antispam/test_otp_guardrails.py -q</automated>
  </verify>
  <done>OTP abuse guard service behavior is covered by deterministic unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add route-level limiter coverage and finalize validation evidence</name>
  <files>tests/test_otp_auth_integration.py, .planning/phases/23-otp-abuse-guardrails/23-VALIDATION.md</files>
  <behavior>
    - Verify and resend POST requests return 429 once the configured route limit is exceeded.
    - Legitimate OTP verification still succeeds when the request rate stays below the abuse thresholds.
    - Validation evidence maps OTPREL-01 and OTPREL-02 to the exact commands used during execution.
  </behavior>
  <action>Extend `tests/test_otp_auth_integration.py` with a limiter-enabled OTP app or dedicated test class that proves POST `/verify-otp` and POST `/verify-otp/resend` return 429 after repeated bursts under low test thresholds, while a normal verify flow still succeeds under the configured limits. Then update `23-VALIDATION.md` so OTPREL-01 and OTPREL-02 map to the exact service and integration pytest commands used in this phase, and mark `nyquist_compliant: true` only after those commands pass during execution.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -k "otp or rate_limit or resend or verify"</automated>
  </verify>
  <done>Phase 23 has deterministic route-level guardrail coverage and an execution-ready validation map.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests -> auth routes and OTP abuse service | Verification must exercise the real limiter and persisted cooldown behaviors instead of mocked happy paths |
| validation map -> execution workflow | Requirement evidence must remain aligned to the commands actually executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-05 | R | Phase 23 validation evidence | mitigate | Map OTPREL requirements to exact pytest commands and use the same commands during execution |
| T-23-06 | D | OTP route guardrails | mitigate | Add integration tests that prove limiter 429 responses occur on verify and resend bursts |
| T-23-07 | T | OTP abuse cooldown sync | mitigate | Add unit tests proving anti-spam cooldown persists and is synchronized onto the active challenge state |
</threat_model>

<verification>
- Run the new OTP abuse service tests.
- Run the new OTP verify/resend guardrail route tests.
- Update 23-VALIDATION.md only after the listed commands pass.
</verification>

<success_criteria>

- OTP abuse guard service behavior is covered by unit tests.
- Route-level limiter behavior is covered by deterministic OTP integration tests.
- Phase 23 validation evidence is traceable to exact commands and requirement IDs.
</success_criteria>

<output>
After completion, create `.planning/phases/23-otp-abuse-guardrails/23-02-SUMMARY.md`
</output>
