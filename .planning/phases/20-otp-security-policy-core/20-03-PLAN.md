---
phase: 20-otp-security-policy-core
plan: 03
type: execute
wave: 3
depends_on: [20-02]
files_modified:
  - tests/test_otp_security_policy.py
  - tests/test_csrf_and_routes.py
  - .planning/phases/20-otp-security-policy-core/20-VALIDATION.md
autonomous: true
requirements: [OTPSEC-01, OTPSEC-02, OTPSEC-03, OTPPOL-01, OTPPOL-02, OTPPOL-03]
must_haves:
  truths:
    - Automated tests prove OTP generation is random and hash-only storage is used.
    - Automated tests prove expiry, lockout, and single-use OTP behavior.
    - Automated tests prove re-issued challenge invalidates previous active challenge.
  artifacts:
    - path: tests/test_otp_security_policy.py
      provides: OTP security policy unit/flow tests for phase requirements
    - path: tests/test_csrf_and_routes.py
      provides: Updated route-level OTP tests aligned with challenge model
    - path: .planning/phases/20-otp-security-policy-core/20-VALIDATION.md
      provides: Validation evidence updates and command coverage map
  key_links:
    - from: tests/test_otp_security_policy.py
      to: routes/auth.py
      via: verify flow behavior assertions
      pattern: /verify-otp
    - from: tests/test_csrf_and_routes.py
      to: models/models.py
      via: OtpChallenge-based test setup
      pattern: OtpChallenge
---

<objective>
Create and align automated tests for all Phase 20 OTP security requirements, then update validation evidence.

Purpose: Prevent regression while Phase 20 removes insecure fallback OTP behavior.
Output: New OTP security test suite, updated route tests, and refreshed validation matrix.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-otp-security-policy-core/20-RESEARCH.md
@.planning/phases/20-otp-security-policy-core/20-VALIDATION.md
@.planning/phases/20-otp-security-policy-core/20-02-PLAN.md
@tests/test_csrf_and_routes.py
@routes/auth.py
@utils/otp_security.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OTP security policy test suite for challenge lifecycle</name>
  <read_first>tests/test_csrf_and_routes.py, routes/auth.py, utils/otp_security.py, models/models.py</read_first>
  <files>tests/test_otp_security_policy.py</files>
  <behavior>
    - Random OTP generation test fails if output is static or malformed.
    - Hash-only storage test fails if plaintext OTP appears in persisted challenge fields.
    - Lifecycle tests fail on missing expiry, lockout, replay prevention, or invalidation behavior.
  </behavior>
  <action>Create tests/test_otp_security_policy.py with focused tests for OTPSEC-01/02/03 and OTPPOL-01/02/03: random six-digit generation, hash storage without plaintext, expiry rejection, lockout after max attempts, successful verify single-use, and superseding invalidation on re-issue. Use deterministic fixtures and freeze-time style helper where needed (no external dependency required).</action>
  <acceptance_criteria>
    - tests/test_otp_security_policy.py exists
    - file contains tests named with otpsec and otppol behaviors
    - file contains assertions for expiry, lockout, single-use, and superseded challenge
    - file does not assert against hardcoded literal demo OTP behavior
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_otp_security_policy.py</automated>
  </verify>
  <done>Dedicated OTP security suite covers all Phase 20 requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update existing auth route tests to use challenge-based OTP setup</name>
  <read_first>tests/test_csrf_and_routes.py, routes/auth.py, models/models.py</read_first>
  <files>tests/test_csrf_and_routes.py</files>
  <behavior>
    - OTP route tests validate challenge-based success and failure branches.
    - Legacy session otp_code assumptions are removed.
  </behavior>
  <action>Refactor OTP-related tests in tests/test_csrf_and_routes.py to create OtpChallenge fixtures instead of writing sess['otp_code']. Keep existing route assertions (redirects/messages) but align setup with pending_otp_challenge_id and challenge lifecycle states. Preserve CSRF coverage tests while updating only OTP behavior expectations tied to Phase 20 policy.</action>
  <acceptance_criteria>
    - tests/test_csrf_and_routes.py no longer writes sess['otp_code'] in OTP tests
    - OTP tests in file set pending_otp_challenge_id session key
    - verify_otp success test asserts challenge becomes used or terminal
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_csrf_and_routes.py -k otp</automated>
  </verify>
  <done>Route-level OTP tests align with the new challenge model and pass.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh validation map with executed commands and outcomes</name>
  <read_first>.planning/phases/20-otp-security-policy-core/20-VALIDATION.md</read_first>
  <files>.planning/phases/20-otp-security-policy-core/20-VALIDATION.md</files>
  <action>Update 20-VALIDATION.md Per-Task Verification Map statuses based on executed tests from Task 1 and Task 2, fill command/runtime observations, and set nyquist_compliant true only when all mapped checks pass.</action>
  <acceptance_criteria>
    - 20-VALIDATION.md includes updated status values for 20-01-01 through 20-03-01
    - validation file records commands used in this phase
    - frontmatter nyquist_compliant is set to true only if test commands pass
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_otp_security_policy.py && python -m pytest tests/test_csrf_and_routes.py -k otp</automated>
  </verify>
  <done>Validation strategy document reflects real test evidence for Phase 20.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures -> auth routes | Simulated untrusted inputs validate security controls |
| verification docs -> release gate | Test evidence informs deployment trust decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | R | Test evidence integrity | mitigate | Require automated commands and explicit status updates in VALIDATION.md |
| T-20-08 | E | OTP bypass regression | mitigate | Add regression tests for replay, lockout, expiry, and superseded challenge paths |
| T-20-09 | I | Sensitive OTP leakage in tests | mitigate | Assert no plaintext OTP persistence assumptions in test setup/output |
</threat_model>

<verification>
- Run dedicated OTP security suite and OTP-focused auth route tests.
- Update validation matrix with actual command outcomes.
</verification>

<success_criteria>

- All Phase 20 OTP requirement IDs have direct automated coverage.
- Existing OTP route tests are migrated away from hardcoded session OTP fallback.
- Validation strategy captures objective evidence for execute and verify phases.
</success_criteria>

<output>
After completion, create `.planning/phases/20-otp-security-policy-core/20-03-SUMMARY.md`
</output>
