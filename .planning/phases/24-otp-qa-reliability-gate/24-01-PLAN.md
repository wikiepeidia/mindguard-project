---
phase: 24-otp-qa-reliability-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_otp_resend_policy.py
autonomous: true
requirements:
  - OTPQA-01
must_haves:
  truths:
    - Resend cooldown behavior is enforced by automated tests.
    - Resend window-cap behavior is enforced by automated tests.
    - Replacement challenge activation keeps the current challenge valid until a resend actually succeeds.
  artifacts:
    - path: tests/test_otp_resend_policy.py
      provides: Dedicated resend policy and replacement challenge unit coverage
  key_links:
    - from: tests/test_otp_resend_policy.py
      to: utils/otp_security.py
      via: direct helper imports and persisted OtpChallenge rows
      pattern: get_resend_otp_policy|prepare_resend_otp_challenge|activate_replacement_otp_challenge
---

<objective>
Close the helper-level OTP resend coverage gap left after Phase 23.

Purpose: Lock the resend lifecycle rules behind direct, database-backed tests so future refactors cannot silently weaken cooldown or replacement semantics.
Output: A dedicated resend policy unit test module covering cooldown, cap, and replacement activation behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@utils/otp_security.py
@models/models.py
@tests/test_otp_security.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resend cooldown and resend-cap unit coverage</name>
  <files>tests/test_otp_resend_policy.py</files>
  <behavior>
    - Test 1: resend becomes allowed once the cooldown window has elapsed.
    - Test 2: resend stays blocked during the cooldown window.
    - Test 3: resend stays blocked once the resend window cap is reached.
  </behavior>
  <action>Create a dedicated `OtpResendPolicyTests` module that uses an isolated in-memory Flask app, real `OtpChallenge` rows, and direct calls into `get_resend_otp_policy()` to prove cooldown and cap outcomes per D-01 through D-03. Do not mock away database state because the resend policy depends on persisted issuance history.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_resend_policy.py -q</automated>
  </verify>
  <done>`tests/test_otp_resend_policy.py` proves allowed vs blocked resend policy outcomes with deterministic database-backed assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add replacement challenge transition coverage</name>
  <files>tests/test_otp_resend_policy.py</files>
  <behavior>
    - Test 1: preparing a resend replacement does not invalidate the current challenge before send success.
    - Test 2: activating the replacement invalidates the previous challenge only at the final handoff point.
  </behavior>
  <action>Extend the same unit module with explicit lifecycle coverage around `prepare_resend_otp_challenge()` and `activate_replacement_otp_challenge()` so the pre-send and post-send states are both enforced. Per D-02, verify current behavior instead of changing the implementation.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_resend_policy.py -q</automated>
  </verify>
  <done>The resend replacement lifecycle is proven safe by tests that assert the current challenge stays active until replacement activation completes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| helper logic -> persisted OTP state | Incorrect resend policy logic could weaken brute-force and replay defenses without changing route code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `utils/otp_security.py` resend policy helpers | mitigate | Add database-backed unit tests for cooldown and resend-cap enforcement |
| T-24-02 | E | replacement challenge lifecycle | mitigate | Add explicit tests proving replacement does not invalidate the current challenge before send success |
</threat_model>

<verification>
Run `python -m pytest tests/test_otp_resend_policy.py -q` and confirm all resend-policy tests pass.
</verification>

<success_criteria>
Helper-level resend policy rules are locked by deterministic tests and no untested replacement lifecycle gap remains.
</success_criteria>

<output>
After completion, create `.planning/phases/24-otp-qa-reliability-gate/24-01-SUMMARY.md`
</output>
