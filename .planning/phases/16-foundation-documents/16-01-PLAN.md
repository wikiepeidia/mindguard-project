---
phase: 16-foundation-documents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/technical/DATABASE.md]
autonomous: true
requirements: [TECH-03]
must_haves:
  truths:
    - "DATABASE.md chứa tất cả 14 bảng từ models.py"
    - "Mỗi bảng có tên, cột, kiểu dữ liệu, constraints"
    - "Quan hệ foreign key được document rõ ràng"
    - "Mermaid ER diagram render được trên GitHub"
  artifacts:
    - path: "docs/technical/DATABASE.md"
      provides: "Complete database schema reference"
      contains: "erDiagram"
  key_links:
    - from: "docs/technical/DATABASE.md"
      to: "models/models.py"
      via: "Schema extracted from SQLAlchemy models"
---

<objective>
Viết lại DATABASE.md với schema thực tế từ models/models.py — 14 bảng nhóm theo domain, kèm Mermaid ER diagram.

Purpose: Làm nền tảng tham chiếu schema cho ARCHITECTURE.md (Phase 17) và tất cả tài liệu sau.
Output: docs/technical/DATABASE.md hoàn chỉnh.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@models/models.py
@docs/technical/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Viết DATABASE.md với schema đầy đủ và ER diagram</name>
  <files>docs/technical/DATABASE.md</files>
  <action>
Viết lại hoàn toàn docs/technical/DATABASE.md (thay thế nội dung template hiện tại). Đọc models/models.py để extract tất cả 14 bảng.

Structure theo thứ tự:
1. Metadata comment block (Owner: @database-expert, Update trigger, Read by)
2. Header: Engine PostgreSQL 15 (NeonDB), ORM SQLAlchemy, Connection via DATABASE_URL
3. Schema Overview — mô tả ngắn về 5 domain groups và quan hệ chính
4. Mermaid erDiagram code fence — tất cả 14 bảng với relationships (per D-03)
5. Tables section — nhóm theo domain (per D-01):
   - **Auth & Users**: registrations
   - **Quiz**: quiz_results, ai_quiz_questions
   - **Scammer Reports**: scam_reports, scammer_reports, scammer_leaderboard
   - **Chatbot**: ai_chat_sessions, ai_chat_messages, chat_support_messages, chat_feedbacks
   - **Anti-Spam**: anti_spam_events, anti_spam_actor_states
   - **Subscriptions**: subscriptions
   - **Audit**: sensitive_access_logs

Mỗi bảng có table với columns: Column | Type | Constraints | Description.
Thứ tự cột: id → foreign keys → data fields → metadata → timestamps.
Ghi rõ relationships (FK references), indexes (nếu có index=True trong model).
Không ghi migration history (per D-02). Không ghi seed data (per D-04).

Ngôn ngữ: Vietnamese prose cho mô tả, English cho technical terms (per CONVENTIONS.md).
Dùng tiếng Việt cho Purpose descriptions của mỗi bảng.
  </action>
  <verify>
    <automated>python -c "content=open('docs/technical/DATABASE.md').read(); tables=['scam_reports','registrations','quiz_results','scammer_reports','scammer_leaderboard','ai_quiz_questions','ai_chat_sessions','ai_chat_messages','chat_support_messages','subscriptions','sensitive_access_logs','anti_spam_events','anti_spam_actor_states','chat_feedbacks']; missing=[t for t in tables if t not in content]; assert not missing, f'Missing tables: {missing}'; assert 'erDiagram' in content, 'Missing Mermaid ER diagram'; print(f'ALL {len(tables)} tables present + ER diagram')"</automated>
  </verify>
  <done>DATABASE.md chứa tất cả 14 bảng nhóm theo domain, mỗi bảng có columns/types/constraints, Mermaid ER diagram render được</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries — this plan creates documentation only, no runtime code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | I (Information Disclosure) | DATABASE.md | mitigate | Do not include actual connection strings, passwords, or sample PII data in schema docs |
</threat_model>

<verification>
- DATABASE.md chứa tất cả 14 bảng từ models.py
- Mermaid erDiagram có trong file
- Không chứa credentials hoặc PII thật
</verification>

<success_criteria>
- 14/14 bảng documented với columns, types, constraints
- ER diagram Mermaid syntax hợp lệ
- Bảng nhóm theo 6 domain groups
</success_criteria>

<output>
After completion, create `.planning/phases/16-foundation-documents/16-01-SUMMARY.md`
</output>
