---
phase: 27-smtp-qa-production-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/models.py
  - database/migrate_anti_spam_phase2.py
  - database/migrate_anti_spam_account_id_length.py
  - tests/antispam/test_otp_guardrails.py
autonomous: true
requirements:
  - SMTPQ-03
must_haves:
  truths:
    - Production `/register` no longer fails because required OTP schema exists in Neon.
    - Production `/verify-otp/resend` no longer crashes when OTP abuse tracking persists actor IDs.
    - The repo contract prevents future drift between OTP actor ID length and anti-spam schema width.
  artifacts:
    - path: models/models.py
      provides: anti-spam account_id columns wide enough for OTP actor identifiers
    - path: database/migrate_anti_spam_account_id_length.py
      provides: idempotent production migration for existing Neon databases
    - path: tests/antispam/test_otp_guardrails.py
      provides: regression check that OTP actor IDs fit anti-spam account_id columns
  key_links:
    - from: services/otp_abuse_guard.py
      to: models/models.py
      via: build_otp_actor_id -> AntiSpamEvent.account_id / AntiSpamActorState.account_id
      pattern: build_otp_actor_id|account_id
    - from: database/migrate_anti_spam_account_id_length.py
      to: anti_spam_events, anti_spam_actor_states
      via: ALTER COLUMN account_id TYPE VARCHAR(128)
      pattern: ALTER TABLE .* account_id TYPE VARCHAR\(128\)
---

<objective>
Repair the production schema blockers uncovered by live SMTP smoke and lock the fix into the repo contract.

Purpose: Make the real Vercel register/resend flow stable by aligning Neon schema with the current OTP and anti-spam code paths.
Output: Production-safe migration path plus regression coverage for OTP actor identifier width.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-smtp-qa-production-verification/27-CONTEXT.md
@.planning/phases/27-smtp-qa-production-verification/27-RESEARCH.md
@models/models.py
@database/migrate_otp_challenges.py
@database/migrate_anti_spam_phase2.py
@services/otp_abuse_guard.py
@tests/antispam/test_otp_guardrails.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align anti-spam account_id width with OTP actor identifiers</name>
  <files>models/models.py, database/migrate_anti_spam_phase2.py, database/migrate_anti_spam_account_id_length.py</files>
  <action>Per D-04, fix the resend blocker at the schema contract instead of truncating actor IDs. Widen `AntiSpamEvent.account_id` and `AntiSpamActorState.account_id` to `String(128)`, update the base anti-spam bootstrap migration for fresh databases, and add an idempotent migration for existing Postgres/Neon environments that still have `VARCHAR(64)`.</action>
  <verify>
    <automated>python -m pytest tests/antispam/test_otp_guardrails.py tests/test_otp_auth_integration.py -q</automated>
  </verify>
  <done>Repo schema definitions and manual migrations all accept the OTP actor ID shape used by the live abuse guard.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the production schema repairs remove the live blockers</name>
  <files>tests/antispam/test_otp_guardrails.py</files>
  <behavior>
    - Test 1: OTP actor identifiers fit both anti-spam `account_id` columns.
    - Test 2: Existing OTP auth integration coverage still passes after the width change.
  </behavior>
  <action>Use `vercel logs` to confirm the live blockers, then run the existing `database/migrate_otp_challenges.py` and the new `database/migrate_anti_spam_account_id_length.py` against the Production database sourced from `vercel env pull`. Keep the repair idempotent so reruns are safe during operations.</action>
  <verify>
    <automated>TMP_ENV=$(mktemp) && vercel env pull "$TMP_ENV" --environment=production --yes && set -a && . <(tr -d '\r' < "$TMP_ENV") && set +a && python database/migrate_otp_challenges.py && python database/migrate_anti_spam_account_id_length.py && rm -f "$TMP_ENV"</automated>
  </verify>
  <done>The production database contains `otp_challenges`, the anti-spam `account_id` columns are widened, and the original live 500s are no longer reproducible.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel runtime -> Neon schema | Production code can fail closed incorrectly if required tables or column widths lag behind repo expectations |
| OTP abuse guard -> anti-spam persistence | Oversized actor IDs can turn guardrail enforcement into runtime failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | D | `otp_challenges` table absence | mitigate | Run the repo's idempotent OTP migration against Production before rerunning live smoke |
| T-27-02 | D | anti-spam `account_id` width mismatch | mitigate | Widen both anti-spam `account_id` columns and lock the contract with a regression test |
| T-27-03 | T | manual migration reruns | mitigate | Keep migrations idempotent and production-safe so repeated ops do not corrupt live state |
</threat_model>

<verification>
Run the OTP guardrail and auth integration tests, then apply the idempotent production migrations sourced from Production envs and confirm the original 500 traces stop appearing.
</verification>

<success_criteria>
The production database matches the current OTP/anti-spam contract closely enough that live register and resend no longer fail with schema-related 500s.
</success_criteria>

<output>
After completion, create `.planning/phases/27-smtp-qa-production-verification/27-01-SUMMARY.md`
</output>
