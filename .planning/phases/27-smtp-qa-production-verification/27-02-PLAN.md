---
phase: 27-smtp-qa-production-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/27-smtp-qa-production-verification/27-01-SUMMARY.md
  - .planning/phases/27-smtp-qa-production-verification/27-02-SUMMARY.md
  - .planning/phases/27-smtp-qa-production-verification/27-VALIDATION.md
  - memories/repo/planning.md
autonomous: true
requirements:
  - SMTPQ-03
must_haves:
  truths:
    - Production register reaches `/verify-otp` on the protected Vercel deployment with the real SMTP mailbox config.
    - Resend shows the expected cooldown guardrail and later succeeds after the cooldown window.
    - Milestone tracking files clearly record that SMTP production cutover evidence is complete.
  artifacts:
    - path: .planning/phases/27-smtp-qa-production-verification/27-VALIDATION.md
      provides: production smoke evidence and runtime blocker diagnosis
    - path: .planning/REQUIREMENTS.md
      provides: SMTPQ-03 marked complete
    - path: .planning/STATE.md
      provides: v1.5 milestone marked complete
  key_links:
    - from: protected production smoke
      to: routes/auth.py
      via: `/register` -> `/verify-otp` -> `/verify-otp/resend`
      pattern: /register|/verify-otp|/verify-otp/resend
    - from: .planning/REQUIREMENTS.md
      to: .planning/ROADMAP.md
      via: SMTPQ-03 completion and Phase 27 status
      pattern: SMTPQ-03|Phase 27
---

<objective>
Capture the final protected production evidence for the SMTP cutover and close out the v1.5 milestone.

Purpose: Convert the live Vercel smoke into durable release evidence and make the repo state reflect that SMTP OTP is working in production with a real mailbox account.
Output: Production validation record plus updated milestone tracking files.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-smtp-qa-production-verification/27-CONTEXT.md
@.planning/phases/27-smtp-qa-production-verification/27-RESEARCH.md
@TODO.mD
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run protected production register and resend smoke with a real mailbox account</name>
  <files>.planning/phases/27-smtp-qa-production-verification/27-VALIDATION.md</files>
  <action>Per D-01 to D-03, use `vercel curl` against the protected production alias with explicit cookie reuse, CSRF parsing, and browser-style GET/POST steps. Capture the sequence where register redirects to `/verify-otp`, immediate resend is guarded by cooldown, and resend succeeds after the cooldown window. Record any schema/runtime blockers and how they were resolved.</action>
  <verify>
    <automated>python -m pytest tests/antispam/test_otp_guardrails.py tests/test_otp_auth_integration.py -q</automated>
  </verify>
  <done>`27-VALIDATION.md` contains enough evidence to show the live SMTP cutover works on Vercel with the real mailbox configuration and deployment protection still enabled.</done>
</task>

<task type="auto">
  <name>Task 2: Mark SMTP production evidence complete across milestone tracking files</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/27-smtp-qa-production-verification/27-01-SUMMARY.md, .planning/phases/27-smtp-qa-production-verification/27-02-SUMMARY.md, memories/repo/planning.md</files>
  <action>Update the v1.5 milestone records so `SMTPQ-03` is complete, Phase 27 is marked done, and the repo memory notes the operational lessons from protected Vercel smoke plus manual production migrations. Keep the wording factual and tied to the actual evidence collected.</action>
  <verify>
    <automated>python -m pytest tests/antispam/test_otp_guardrails.py tests/test_otp_auth_integration.py -q</automated>
  </verify>
  <done>The repo's planning files show that v1.5 is complete and the remaining SMTP work is explicitly deferred beyond the current milestone.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protected deployment -> smoke tooling | The smoke tool must preserve auth, cookies, and CSRF exactly or it will generate false negatives |
| release evidence -> future operators | Incomplete phase records can make a live production fix look undocumented or unrepeatable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-04 | R | production smoke evidence | mitigate | Record the actual protected smoke behavior, including cooldown redirect and later resend success |
| T-27-05 | T | milestone tracking files | mitigate | Update requirements, roadmap, and state from the real production evidence instead of assumptions |
| T-27-06 | D | protected smoke tooling | mitigate | Use explicit GET/POST steps with cookies and CSRF parsing instead of relying on redirect replay |
</threat_model>

<verification>
Keep the OTP regression suite green and capture the protected production smoke results in `27-VALIDATION.md`, including the final resend success after cooldown.
</verification>

<success_criteria>
The repo contains durable evidence that SMTP OTP is working on the protected Vercel production deployment, and all v1.5 milestone tracking files show completion.
</success_criteria>

<output>
After completion, create `.planning/phases/27-smtp-qa-production-verification/27-02-SUMMARY.md`
</output>
