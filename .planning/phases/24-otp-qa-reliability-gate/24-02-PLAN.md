---
phase: 24-otp-qa-reliability-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - tests/test_otp_auth_integration.py
  - .planning/phases/24-otp-qa-reliability-gate/24-VALIDATION.md
autonomous: true
requirements:
  - OTPQA-02
  - OTPQA-03
must_haves:
  truths:
    - Resend send failure leaves the existing OTP challenge intact.
    - Concurrent verify attempts cannot both succeed against one challenge.
    - The broader OTP regression suite passes after the new coverage lands.
  artifacts:
    - path: tests/test_otp_auth_integration.py
      provides: Resend failure rollback and concurrent verify integration tests
    - path: .planning/phases/24-otp-qa-reliability-gate/24-VALIDATION.md
      provides: Executed Phase 24 validation evidence
  key_links:
    - from: tests/test_otp_auth_integration.py
      to: routes/auth.py
      via: Flask client requests against `/verify-otp` and `/verify-otp/resend`
      pattern: resend|concurrent|verify
---

<objective>
Close the remaining route and release-gate QA gaps for OTP.

Purpose: Prove the auth flow behaves correctly during resend delivery failure and concurrent verify races, then record the exact commands that cleared the OTP regression gate.
Output: Integration tests for resend rollback and concurrent verify plus a completed validation artifact for Phase 24.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@routes/auth.py
@services/otp_email_delivery.py
@tests/test_otp_auth_integration.py
@tests/test_csrf_and_routes.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resend failure rollback integration coverage</name>
  <files>tests/test_otp_auth_integration.py</files>
  <behavior>
    - Test 1: resend send failure keeps the current challenge active.
    - Test 2: resend send failure does not leave the session pointing at a half-activated replacement challenge.
  </behavior>
  <action>Extend the OTP auth integration suite with a resend-specific failure test that patches `routes.auth.send_otp_email` and proves the route fails closed while preserving the original challenge state per D-02 and D-05. Reuse the existing pending-session helpers rather than creating a second harness.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -k "resend or concurrent" -q</automated>
  </verify>
  <done>The resend route is covered for provider failure rollback and the persisted/session state remains consistent after failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add concurrent verify single-success coverage</name>
  <files>tests/test_otp_auth_integration.py</files>
  <behavior>
    - Test 1: only one of two concurrent verify submissions succeeds.
    - Test 2: the losing request observes an already-used or otherwise invalid challenge outcome.
  </behavior>
  <action>Add a two-client integration test that seeds the same pending OTP challenge into both sessions and posts the same valid code through both clients. Per D-04, keep this as a real Flask request flow backed by persisted challenge state so replay resistance is verified at the route boundary.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -k "resend or concurrent" -q</automated>
  </verify>
  <done>Concurrent OTP verify replay is covered and only one successful verification can consume the same challenge.</done>
</task>

<task type="auto">
  <name>Task 3: Capture focused and broad OTP validation evidence</name>
  <files>.planning/phases/24-otp-qa-reliability-gate/24-VALIDATION.md</files>
  <action>Record the exact focused commands and the broader OTP regression command executed for Phase 24, including pass counts and residual warnings. Per D-05, document both the narrow Phase 24 gate and the broader OTP suite so milestone closeout is evidence-backed.</action>
  <verify>
    <automated>python -m pytest tests/test_otp_email_delivery.py tests/test_otp_security.py tests/test_otp_resend_policy.py tests/test_otp_auth_integration.py tests/test_csrf_and_routes.py -q</automated>
  </verify>
  <done>`24-VALIDATION.md` contains the executed commands, pass counts, and residual notes for the full Phase 24 QA gate.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `/verify-otp` | Concurrent or replayed requests compete to consume the same OTP challenge |
| client -> `/verify-otp/resend` | Provider send failure must not corrupt the active challenge/session state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-03 | R | `/verify-otp` concurrent submission path | mitigate | Add integration coverage proving only one request can consume a challenge |
| T-24-04 | D | `/verify-otp/resend` failure path | mitigate | Add rollback test proving delivery failure does not strand the user in broken challenge state |
| T-24-05 | T | release-gate evidence | mitigate | Capture exact focused and broad pytest commands in `24-VALIDATION.md` |
</threat_model>

<verification>
Run `python -m pytest tests/test_otp_auth_integration.py -k "resend or concurrent" -q` and then the broader OTP regression suite.
</verification>

<success_criteria>
The missing route/integration QA gaps are closed and the broader OTP suite passes with documented evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/24-otp-qa-reliability-gate/24-02-SUMMARY.md`
</output>
