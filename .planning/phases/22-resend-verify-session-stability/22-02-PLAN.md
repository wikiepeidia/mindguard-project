---
phase: 22-resend-verify-session-stability
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - tests/test_otp_auth_integration.py
  - tests/test_csrf_and_routes.py
  - .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md
autonomous: true
requirements: [OTPRES-01, OTPRES-02, OTPSES-01]
must_haves:
  truths:
    - Resend success, resend denial, and refresh-safe redirect behavior are covered by deterministic automated tests.
    - The new resend endpoint remains CSRF-protected.
    - Validation evidence maps every Phase 22 requirement to runnable commands.
  artifacts:
    - path: tests/test_otp_auth_integration.py
      provides: Route and session regression coverage for resend and verify refresh behavior
    - path: tests/test_csrf_and_routes.py
      provides: CSRF coverage for the resend endpoint
    - path: .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md
      provides: Requirement-to-command validation map for Phase 22
  key_links:
    - from: tests/test_otp_auth_integration.py
      to: routes/auth.py
      via: Flask test client requests to /verify-otp and /verify-otp/resend
      pattern: /verify-otp/resend
    - from: tests/test_csrf_and_routes.py
      to: routes/auth.py
      via: POST CSRF enforcement checks for resend
      pattern: verify-otp/resend
    - from: .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md
      to: tests/test_otp_auth_integration.py
      via: quick and full pytest commands
      pattern: python -m pytest
---

<objective>
Lock the Phase 22 contract with deterministic route/session tests and a validation map that is ready for execution-time evidence.

Purpose: Ensure resend/session behavior does not regress once the new auth flow changes land.
Output: Route-level resend/verify tests, resend CSRF coverage, and updated validation strategy.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-resend-verify-session-stability/22-CONTEXT.md
@.planning/phases/22-resend-verify-session-stability/22-RESEARCH.md
@.planning/phases/22-resend-verify-session-stability/22-VALIDATION.md
@tests/test_otp_auth_integration.py
@tests/test_csrf_and_routes.py
@routes/auth.py
@templates/verify_otp.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend route/session integration coverage for resend and refresh behavior</name>
  <read_first>tests/test_otp_auth_integration.py, routes/auth.py, utils/otp_security.py, services/otp_email_delivery.py, templates/verify_otp.html, .planning/phases/22-resend-verify-session-stability/22-CONTEXT.md</read_first>
  <files>tests/test_otp_auth_integration.py</files>
  <action>Extend tests/test_otp_auth_integration.py with deterministic Flask-client coverage for Phase 22. Add cases that patch `routes.auth.send_otp_email` and seed `OtpChallenge` rows to verify: GET `/verify-otp` returns 200 when `pending_registration` and a valid pending challenge still exist; GET `/verify-otp` redirects to `/register` when pending session is missing; GET `/verify-otp` redirects safely when the referenced challenge is expired; POST `/verify-otp/resend` redirects safely when pending session state is missing; POST `/verify-otp/resend` with a valid pending session issues a replacement challenge, invalidates the previous active challenge only after resend success, and updates `pending_otp_challenge_id`; POST resend during cooldown or after the configured cap keeps the current challenge reference unchanged and flashes a wait message.</action>
  <acceptance_criteria>
    - tests/test_otp_auth_integration.py contains a resend success test
    - tests/test_otp_auth_integration.py contains a resend missing-session denial test
    - tests/test_otp_auth_integration.py contains a resend cooldown denial test
    - tests/test_otp_auth_integration.py contains a missing-session GET /verify-otp redirect test
    - tests/test_otp_auth_integration.py contains an expired-challenge GET /verify-otp redirect test
    - tests/test_otp_auth_integration.py patches `routes.auth.send_otp_email`
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_otp_auth_integration.py -k "resend or verify"</automated>
  </verify>
  <done>Phase 22 resend and refresh-safe session behavior is covered by deterministic automated route tests.</done>
</task>

<task type="auto">
  <name>Task 2: Add resend CSRF regression coverage and finish the validation map</name>
  <read_first>tests/test_csrf_and_routes.py, .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md, routes/auth.py, tests/test_otp_auth_integration.py</read_first>
  <files>tests/test_csrf_and_routes.py, .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md</files>
  <action>Update tests/test_csrf_and_routes.py to cover the new POST `/verify-otp/resend` endpoint: without a CSRF token it must return 400, and with a valid CSRF token plus seeded pending session it must reach the route logic. Then finish 22-VALIDATION.md by mapping OTPRES-01, OTPRES-02, and OTPSES-01 to the exact pytest commands added in this phase, and mark `nyquist_compliant: true` only after the commands listed in the file are the commands used for execution verification.</action>
  <acceptance_criteria>
    - tests/test_csrf_and_routes.py contains a resend-without-CSRF rejection test
    - tests/test_csrf_and_routes.py contains a resend-with-CSRF success-path test
    - .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md lists OTPRES-01
    - .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md lists OTPRES-02
    - .planning/phases/22-resend-verify-session-stability/22-VALIDATION.md lists OTPSES-01
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_csrf_and_routes.py -k "verify_otp or resend" && python -m pytest tests/test_otp_auth_integration.py -k "resend or verify"</automated>
  </verify>
  <done>The resend endpoint remains CSRF-protected and the validation file is ready to become execution evidence.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests -> auth routes | Regression checks validate security-sensitive route behavior by driving real Flask endpoints |
| validation map -> execution workflow | Phase verification depends on exact command lines staying aligned to implemented tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | T | resend POST CSRF posture | mitigate | Add explicit CSRF regression tests for `/verify-otp/resend` with and without token |
| T-22-05 | R | Phase 22 validation evidence | mitigate | Map each requirement to named pytest commands inside 22-VALIDATION.md and use those commands during execution |
</threat_model>

<verification>
- Run the new resend/verify integration tests.
- Run the resend CSRF regression checks.
- Confirm the validation map uses the same commands that execution will run.
</verification>

<success_criteria>

- Resend success, denial, and refresh-state regressions are all covered by automated tests.
- The resend endpoint cannot bypass CSRF protection.
- Phase 22 validation evidence is traceable to exact requirement IDs and pytest commands.
</success_criteria>

<output>
After completion, create `.planning/phases/22-resend-verify-session-stability/22-02-SUMMARY.md`
</output>