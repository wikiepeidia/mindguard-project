---
phase: 21-production-otp-email-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config.py
  - services/otp_email_delivery.py
  - services/__init__.py
autonomous: true
requirements: [OTPMAIL-03, OTPMAIL-01]
must_haves:
  truths:
    - OTP email delivery provider can be selected as Resend API via runtime environment.
    - OTP mail credentials are read from environment variables only.
    - Delivery service returns explicit success/failure outcomes for auth flow handling.
  artifacts:
    - path: config.py
      provides: Runtime config keys for Resend provider and OTP email delivery timeouts
    - path: services/otp_email_delivery.py
      provides: Provider client and send_otp_email contract for OTP messages
  key_links:
    - from: routes/auth.py
      to: services/otp_email_delivery.py
      via: send_otp_email call after challenge issuance
      pattern: send_otp_email
    - from: services/otp_email_delivery.py
      to: config.py
      via: EMAIL_PROVIDER and RESEND_* settings
      pattern: RESEND_API_KEY
---

<objective>
Define the production email delivery foundation for Phase 21 using Resend API and environment-only secrets.

Purpose: Establish a secure and deterministic OTP delivery contract before route-level integration.
Output: Config keys and OTP email delivery service ready for auth integration.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-production-otp-email-delivery/21-CONTEXT.md
@config.py
@services/
@routes/auth.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Resend runtime config contract and env validation guards</name>
  <read_first>config.py, .planning/phases/21-production-otp-email-delivery/21-CONTEXT.md</read_first>
  <files>config.py</files>
  <behavior>
    - EMAIL_PROVIDER supports resend_api as explicit provider selection.
    - RESEND_API_KEY and RESEND_FROM_EMAIL are loaded from environment variables.
    - Missing required keys produce explicit configuration errors for OTP send path.
  </behavior>
  <action>Update config.py with provider-specific keys for OTP delivery (`EMAIL_PROVIDER`, `RESEND_API_KEY`, `RESEND_FROM_EMAIL`, `OTP_EMAIL_TIMEOUT_SECONDS`). Keep production credentials environment-based only and remove any possibility of hardcoded fallback secrets for Resend provider. Provide helper/readiness check utility used by email service to fail closed when configuration is incomplete.</action>
  <verify>
    <automated>python -m py_compile config.py</automated>
  </verify>
  <done>Config exposes Resend delivery settings and validation logic aligned to OTPMAIL-03.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create OTP email delivery service backed by Resend API</name>
  <read_first>services/__init__.py, routes/auth.py, config.py</read_first>
  <files>services/otp_email_delivery.py, services/__init__.py</files>
  <behavior>
    - send_otp_email(email, otp_code, context) returns structured status for success/failure.
    - Service uses Resend HTTP API with timeout and single retry policy.
    - Service never logs OTP plaintext or API key.
  </behavior>
  <action>Create services/otp_email_delivery.py with provider abstraction for current `resend_api` mode. Implement `send_otp_email` (plain-text body required, optional HTML), timeout-aware HTTP request, and one bounded retry for transient network errors. Return normalized result object (`ok`, `category`, `message`) so route layer can map user-facing guidance cleanly. Export service symbols via services/__init__.py for simple imports from routes/auth.py.</action>
  <verify>
    <automated>python -m py_compile services/otp_email_delivery.py services/__init__.py</automated>
  </verify>
  <done>Resend-backed OTP email service is available for route integration without leaking secrets.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app runtime -> Resend API | Outbound OTP payload crosses external network boundary |
| env secrets -> config/service | Sensitive API key values loaded into runtime memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | I | OTP email service logging | mitigate | Ensure logs exclude OTP plaintext and secrets; log only status/category |
| T-21-02 | D | Resend outage/timeout in request path | mitigate | Add bounded timeout + retry and normalized error category return |
| T-21-03 | E | Misconfigured provider credentials | mitigate | Validate required env keys and fail closed before send attempt |
</threat_model>

<verification>
- Compile configuration and service modules.
- Verify provider contract is Resend-first and environment-only for credentials.
</verification>

<success_criteria>
- Resend provider contract exists and is selectable via config.
- OTP mail service can return deterministic outcomes for auth route handling.
- No hardcoded credentials exist for OTP email provider path.
</success_criteria>

<output>
After completion, create `.planning/phases/21-production-otp-email-delivery/21-01-SUMMARY.md`
</output>
