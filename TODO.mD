Giao diện có 2 phần: 
    1, phần giao diện tố cáo scammer giống như web checkscam.vn (có thể copy)
            + tạo tk mk (minh làm)
            + tố cáo ng dùng thì ng dùng càn đc bảo mật và mã hóa để ko check đc 
            + và scammer nào đc tố cáo nhiều nhất thì cho lên bảng vàng truy nã từ nhớ tạo bảng top list scammer 
            + Có 1 chat box nho nhỏ để hỗ trợ ng dùng tố cáo và tạo 1 cái line nhỏ để tố cáo cần những tố và đưa những bằng chứng đầy dủ xác thực thì duyệt ko thì từ chối tự động 
    2, phần ăn tiền nhất là giao diện có chatbot hỗ trợ, phần này có chức năng tạo câu hỏi cho người dùng để từ đó phòng và tránh khỏi các 
       người đi scam và data tạo câu hỏi từ các đơn tố cáo người dùng và sau khi ng dùng kiểm tra xong thì đưa ra ng dùng có thể tránhđc bao pt scam  
    3, chia các file Py, html, bla bla ra từng file riêng cho dễ tìm và bên trong chia phân các file rõ ràng 


-sửa : luc