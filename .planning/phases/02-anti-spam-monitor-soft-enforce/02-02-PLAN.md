---
phase: 02-anti-spam-monitor-soft-enforce
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - routes/scammer.py
  - tests/antispam/test_monitor_mode.py
  - tests/antispam/test_soft_enforce.py
autonomous: true
requirements:
  - ABUS-03
  - ABUS-01
  - ABUS-02
must_haves:
  truths:
    - "Monitor mode ghi telemetry day du nhung khong chan submit hop le."
    - "Soft-enforce su dung dung decision engine va chi kich hoat cooldown khi vuot nguong."
    - "Co the chuyen monitor -> soft_enforce bang config ma khong doi logic tinh rui ro."
  artifacts:
    - path: "routes/scammer.py"
      provides: "Pre-write anti-spam gate tich hop voi report submission flow."
    - path: "tests/antispam/test_monitor_mode.py"
      provides: "Tests xac minh monitor mode cho phep nhung van ghi event."
    - path: "tests/antispam/test_soft_enforce.py"
      provides: "Tests xac minh soft_enforce cooldown behavior."
  key_links:
    - from: "routes/scammer.py"
      to: "services/anti_spam.py"
      via: "evaluate/record decision truoc db write"
      pattern: "anti_spam.*evaluate|record"
    - from: "routes/scammer.py"
      to: "config.py"
      via: "ABUS_MODE gate monitor vs soft_enforce"
      pattern: "ABUS_MODE"
---

<objective>
Wire anti-spam decision engine vao flow POST report theo monitor-first, sau do soft-enforce bang config toggle de dat ABUS-03 ma khong pha UX hop le.

Purpose: Dua anti-spam vao runtime flow that su, thu telemetry an toan truoc enforce, giu false-positive thap.
Output: Route integration + monitor/soft-enforce tests cho behavior rollout.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-anti-spam-monitor-soft-enforce/2-CONTEXT.md
@.planning/phases/02-anti-spam-monitor-soft-enforce/02-RESEARCH.md
@.planning/phases/02-anti-spam-monitor-soft-enforce/02-01-SUMMARY.md
@routes/scammer.py
@services/anti_spam.py

<interfaces>
From services/anti_spam.py (from plan 02-01):
```python
def collect_signals(session, request):
    ...

def evaluate(route_key, signals, now=None):
    ...

def record_event(decision, signals, route_key):
    ...

```

From routes/scammer.py:
```python
@scammer_bp.route("/report", methods=["GET", "POST"])
def report_scammer():
    ...
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tao test monitor vs soft-enforce integration cho report route</name>
  <read_first>
    - .planning/phases/02-anti-spam-monitor-soft-enforce/02-VALIDATION.md
    - tests/privacy/test_admin_export_policy.py
    - routes/scammer.py
  </read_first>
  <files>tests/antispam/test_monitor_mode.py, tests/antispam/test_soft_enforce.py</files>
  <behavior>
    - Test 1: monitor mode cho phep submit ngay ca khi decision co cooldown flag, nhung van ghi event.
    - Test 2: soft_enforce mode tu choi submit khi cooldown active va khong ghi report moi vao DB.
    - Test 3: soft_enforce mode khong chan hard-block account sach khi chi co IP/cookie xau o muc medium.
  </behavior>
  <action>Viet integration tests cho POST /scammer/report voi fixture de tao burst submissions. Test can cover ca monitor va soft_enforce, verify behavior dong nhat voi locked decision uu tien false-positive avoidance.</action>
  <verify>
    <automated>python -m unittest tests/antispam/test_monitor_mode.py tests/antispam/test_soft_enforce.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Co test cho ca hai mode monitor va soft_enforce.
    - Co assertion ro ve DB write allowed/blocked theo mode.
    - Co assertion ro ve event logging path khi monitor mode.
  </acceptance_criteria>
  <done>Regression integration cho rollout mode da co va chay duoc.</done>
</task>

<task type="auto">
  <name>Task 2: Tich hop anti-spam pre-write gate vao POST report flow</name>
  <read_first>
    - routes/scammer.py
    - services/anti_spam.py
    - .planning/phases/02-anti-spam-monitor-soft-enforce/2-CONTEXT.md
  </read_first>
  <files>routes/scammer.py</files>
  <action>Chen anti-spam evaluation ngay truoc khoi luu ScammerReport: collect signals tu session/request (account neu co, reporter_hash/cookie, IP), evaluate decision, record telemetry event. Neu ABUS_MODE=monitor thi tiep tuc luu report nhu cu; neu ABUS_MODE=soft_enforce va decision.should_cooldown=true thi ngan luu report va tra user ve trang report voi trang thai cooldown. Khong thay doi behavior CAPTCHA fallback hien co.</action>
  <verify>
    <automated>python -m unittest tests/antispam/test_monitor_mode.py tests/antispam/test_soft_enforce.py -v</automated>
  </verify>
  <acceptance_criteria>
    - Diem enforce nam truoc DB write path cua report route.
    - Monitor mode khong block submit hop le.
    - Soft-enforce block dung nguong va van ghi telemetry event.
  </acceptance_criteria>
  <done>ABUS-03 monitor->soft-enforce duoc operationalize o route report.</done>
</task>

</tasks>

<verification>
- python -m unittest tests/antispam/test_monitor_mode.py tests/antispam/test_soft_enforce.py -v
</verification>

<success_criteria>

- Route report su dung anti-spam service theo monitor-first progression.
- Config mode toggle co tac dung thuc te va khong can patch logic scoring.
- False-positive duoc han che theo locked policy (khong hard-block don tin hieu).
</success_criteria>

<output>
After completion, create `.planning/phases/02-anti-spam-monitor-soft-enforce/02-02-SUMMARY.md`
</output>
