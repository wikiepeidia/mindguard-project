---
phase: 19-verification-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/technical/DATABASE.md
  - docs/technical/ARCHITECTURE.md
  - docs/technical/API.md
  - documents/SOP/SOP_BAO_CAO.md
  - documents/SOP/SOP_VAN_HANH.md
  - documents/SOP/SOP_QUAN_TRI.md
autonomous: true
requirements: [CONV-01, DB-01, DB-02, ARCH-01, API-01, API-02, SOP-01, SOP-02, SOP-03]

must_haves:
  truths:
    - "Mỗi tên bảng trong DATABASE.md khớp với __tablename__ trong models/models.py"
    - "Mỗi route path trong API.md khớp với decorator @route() trong routes/*.py"
    - "Mỗi config key trong tài liệu khớp với config.py"
    - "Không còn PLACEHOLDER nào chưa được xử lý (image placeholders được ghi nhận chấp nhận)"
  artifacts:
    - path: "docs/technical/DATABASE.md"
      provides: "Verified table names and model references"
    - path: "docs/technical/API.md"
      provides: "Verified route paths and methods"
  key_links:
    - from: "docs/technical/DATABASE.md"
      to: "models/models.py"
      via: "table name cross-reference"
    - from: "docs/technical/API.md"
      to: "routes/*.py"
      via: "route path cross-reference"
---

<objective>
Cross-check all facts in v1.3 documentation against the actual codebase and fix any mismatches.

Purpose: Ensure every table name, route path, config key, and model name in documentation matches reality (SC-1). Audit PLACEHOLDERs (SC-2).
Output: All 8 v1.3 docs verified accurate. PLACEHOLDER audit complete.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-verification-maintenance/19-CONTEXT.md

@models/models.py
@routes/admin.py
@routes/api.py
@routes/auth.py
@routes/chatbot.py
@routes/library.py
@routes/main.py
@routes/quiz.py
@routes/scammer.py
@config.py
@app.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated cross-check of all docs facts vs codebase</name>
  <files>docs/technical/DATABASE.md, docs/technical/API.md, docs/technical/ARCHITECTURE.md, documents/SOP/SOP_BAO_CAO.md, documents/SOP/SOP_VAN_HANH.md, documents/SOP/SOP_QUAN_TRI.md</files>
  <action>
Per D-01: Run automated verification against codebase. Execute these checks inline (no standalone script needed — one-time verification):

**1. Table name check (DATABASE.md vs models/models.py):**

- Extract all `__tablename__` values from `models/models.py` (expected: 14 tables).
- Extract all table names referenced in `docs/technical/DATABASE.md`.
- Compare: every table in docs must exist in code, every table in code must be in docs.

**2. Route path check (API.md vs routes/*.py):**

- Extract all `@*.route(...)` definitions from `routes/*.py` (8 files). Note: route paths combine blueprint prefix + route decorator.
  - Blueprint prefixes from `app.py`: admin="/admin", chatbot="/chatbot", scammer="/scammer", api="/api/v1", auth/main/quiz/library = no prefix or as defined.
- Extract all route paths from `docs/technical/API.md`.
- Compare: every documented route must exist in code, flag undocumented routes.

**3. Config key check (SOP_VAN_HANH.md + ARCHITECTURE.md vs config.py):**

- Extract all config/env var references from docs.
- Verify each exists in `config.py` or `app.py`.

**4. Model class name check:**

- Extract all class names from `models/models.py`.
- Verify they appear correctly in DATABASE.md and ARCHITECTURE.md.

**5. PLACEHOLDER audit (per D-04):**

- `grep -rn PLACEHOLDER docs/ documents/SOP/` on all v1.3 docs.
- v1.3 scope: docs/technical/*.md + documents/SOP/SOP_*.md.
- SOP_BAO_CAO.md PLACEHOLDER_HINH_* are image placeholders — acceptable, document this finding.
- HUONG_DAN_BAO_CAO_NGUOI_DUNG.md is NOT v1.3 scope — ignore.
- Any other PLACEHOLDERs in v1.3 docs must be resolved.

Output a list of all mismatches/issues found.
  </action>
  <verify>
    <automated>cd "c:\Users\wikiepeidia\OneDrive - caugiay.edu.vn\bài tập\usth\GEN14\GROUP project\mindguard_flask_v2" && python -c "
import re

# Check table names

with open('models/models.py') as f: code = f.read()
tables_code = set(re.findall(r\"**tablename**\s*=\s*['\"][\w+]('\")\", code))
with open('docs/technical/DATABASE.md') as f: db_doc = f.read()
tables_doc = set(re.findall(r'\|\s*`?(\w+)`?\s*\|', db_doc)) & tables_code
missing = tables_code - tables_doc
if missing: print(f'MISSING tables in docs: {missing}')
else: print(f'All {len(tables_code)} tables documented')

# Check no non-image PLACEHOLDERs in v1.3 docs

import subprocess
r = subprocess.run(['grep','-rn','PLACEHOLDER','docs/technical/'], capture_output=True, text=True)
if r.stdout.strip(): print(f'PLACEHOLDERs in tech docs: {r.stdout.strip()}')
else: print('No PLACEHOLDERs in tech docs')
print('PASS')
"</automated>
  </verify>
  <done>Complete mismatch report produced. All facts catalogued with pass/fail status.</done>
</task>

<task type="auto">
  <name>Task 2: Fix all mismatches found in cross-check</name>
  <files>docs/technical/DATABASE.md, docs/technical/API.md, docs/technical/ARCHITECTURE.md, documents/SOP/SOP_BAO_CAO.md, documents/SOP/SOP_VAN_HANH.md, documents/SOP/SOP_QUAN_TRI.md</files>
  <action>
For each mismatch found in Task 1:
- If a table name is wrong/missing in DATABASE.md → fix to match models.py __tablename__.
- If a route path is wrong in API.md → fix to match actual route decorator + blueprint prefix.
- If a config key is wrong in any SOP → fix to match config.py.
- If a model class name is wrong → fix to match models.py class name.

For PLACEHOLDERs:

- SOP_BAO_CAO.md PLACEHOLDER_HINH_01 through _04: Keep as-is (image placeholders per D-04). Add a comment at top of file noting these are intentional image placeholders awaiting screenshots.
- If any other PLACEHOLDERs found in v1.3 docs: replace with actual content from codebase.

If Task 1 found zero mismatches: verify this claim by spot-checking 3 random facts (1 table, 1 route, 1 config key) manually, then document "Zero mismatches confirmed."
  </action>
  <verify>
    <automated>cd "c:\Users\wikiepeidia\OneDrive - caugiay.edu.vn\bài tập\usth\GEN14\GROUP project\mindguard_flask_v2" && grep -c "PLACEHOLDER" docs/technical/CONVENTIONS.md docs/technical/DATABASE.md docs/technical/DECISIONS.md docs/technical/ARCHITECTURE.md docs/technical/API.md documents/SOP/SOP_VAN_HANH.md documents/SOP/SOP_QUAN_TRI.md 2>/dev/null && echo "SOP_BAO_CAO image placeholders (acceptable):" && grep -c "PLACEHOLDER_HINH" documents/SOP/SOP_BAO_CAO.md</automated>
  </verify>
  <done>All mismatches fixed. Zero incorrect facts remain. PLACEHOLDER audit documented (image placeholders in SOP_BAO_CAO.md accepted).</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs ↔ codebase | Documentation references code artifacts — drift = misinformation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | docs/ | accept | Docs are public-facing technical docs, no secrets. Redaction rules from Phase 15 CONVENTIONS.md already applied. |
</threat_model>

<verification>
- All 14 table names in DATABASE.md match models/models.py __tablename__
- All 42+ route paths in API.md match routes/*.py decorators + blueprint prefixes
- All config keys in SOPs match config.py
- grep PLACEHOLDER across v1.3 docs returns only SOP_BAO_CAO.md PLACEHOLDER_HINH_* (accepted)
</verification>

<success_criteria>
SC-1: Every fact in docs cross-checked — no incorrect or outdated info.
SC-2: No unprocessed PLACEHOLDERs (image placeholders documented as accepted).
</success_criteria>

<output>
After completion, create `.planning/phases/19-verification-maintenance/19-01-SUMMARY.md`
</output>
