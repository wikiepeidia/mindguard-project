---
phase: 18-operational-sops
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - documents/SOP/SOP_BAO_CAO.md
autonomous: true
requirements:
  - SOP-01
must_haves:
  truths:
    - "SOP_BAO_CAO.md có routes đúng với prefix /admin/"
    - "Không còn reference đến SQLite"
    - "Cross-references đến API.md và DATABASE.md"
  artifacts:
    - path: "documents/SOP/SOP_BAO_CAO.md"
      provides: "Updated reporting SOP with current routes"
      contains: "/admin/"
  key_links:
    - from: "documents/SOP/SOP_BAO_CAO.md"
      to: "docs/technical/API.md"
      via: "Markdown link trong Tài liệu liên quan"
---

<objective>
Cập nhật SOP_BAO_CAO.md cho đúng routes và database references hiện tại.

Purpose: SOP báo cáo phản ánh đúng hệ thống hiện tại, không còn references lỗi thời.
Output: `documents/SOP/SOP_BAO_CAO.md` cập nhật.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/18-operational-sops/18-CONTEXT.md
@documents/SOP/SOP_BAO_CAO.md
@docs/technical/API.md
@docs/technical/DATABASE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cập nhật routes và references trong SOP_BAO_CAO.md</name>
  <files>documents/SOP/SOP_BAO_CAO.md</files>
  <action>
Per D-01 — Cập nhật có chọn lọc:

1. **Section 4 (Màn hình và đường dẫn)**: Sửa routes cho đúng prefix:
   - `/admin/scammer-reports` (giữ nguyên — đã đúng)
   - `POST /approve-report/<report_id>` → `POST /admin/approve-report/<report_id>`
   - `POST /reject-report/<report_id>` → `POST /admin/reject-report/<report_id>`
   - `GET /export-dataset` → `GET/POST /admin/export-dataset`

2. **Thêm cross-references**: Thêm section "11. Tài liệu liên quan" cuối file với links:
   - `docs/technical/API.md` — Chi tiết endpoints admin (xem section "admin")
   - `docs/technical/DATABASE.md` — Schema bảng `scammer_reports`, `scam_reports`
   - `documents/SOP/SOP_QUAN_TRI.md` — Hướng dẫn quản trị tổng thể

3. **Kiểm tra không còn SQLite references**: Grep cho "sqlite", "SQLite", ".db" — sửa nếu có.

4. **Không thay đổi** nội dung quy trình (sections 5-10) — vẫn chính xác.
  </action>
  <verify>
    <automated>python -c "
c=open('documents/SOP/SOP_BAO_CAO.md',encoding='utf-8').read()
checks=[
  ('/admin/approve-report' in c, 'Correct approve route'),
  ('/admin/reject-report' in c, 'Correct reject route'),
  ('/admin/export-dataset' in c, 'Correct export route'),
  ('API.md' in c or 'api.md' in c, 'Cross-ref API.md'),
  ('DATABASE.md' in c or 'database.md' in c, 'Cross-ref DATABASE.md'),
  ('sqlite' not in c.lower() or 'SQLite' not in c, 'No SQLite references'),
]
for ok,name in checks:
  print(f'  {\"PASS\" if ok else \"FAIL\"}: {name}')
passed=sum(1 for ok,_ in checks if ok)
print(f'\n{passed}/{len(checks)} passed')
assert passed==len(checks)
"</automated>
  </verify>
  <done>SOP_BAO_CAO.md có routes đúng prefix /admin/, cross-references đến API.md và DATABASE.md, không còn SQLite references.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ output | Documentation only — no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-18-01 | Information Disclosure | SOP files | mitigate | Apply redaction rules — no real secrets in SOPs |
</threat_model>

<verification>
SOP_BAO_CAO.md routes đúng, cross-references đầy đủ, không có SQLite references.
</verification>

<success_criteria>
SOP_BAO_CAO.md cập nhật đúng cho hệ thống hiện tại.
</success_criteria>

<output>
After completion, create `.planning/phases/18-operational-sops/18-01-SUMMARY.md`
</output>
