---
phase: 20-otp-security-policy-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - models/models.py
  - config.py
  - utils/otp_security.py
  - database/migrate_otp_challenges.py
autonomous: true
requirements: [OTPSEC-01, OTPSEC-02]
user_setup:
  - service: environment
    why: Pepper secret for OTP hashing in production
    env_vars:
      - name: OTP_PEPPER
        source: Deployment environment variable (Vercel/local env)
must_haves:
  truths:
    - OTP generation uses cryptographically secure six-digit values and has no static fallback.
    - OTP persistence stores hash metadata only (no plaintext OTP persisted).
  artifacts:
    - path: models/models.py
      provides: OtpChallenge ORM model with lifecycle and policy fields
    - path: utils/otp_security.py
      provides: OTP issue/hash/verify helper functions
    - path: database/migrate_otp_challenges.py
      provides: Manual script to create otp_challenges schema
    - path: config.py
      provides: OTP security policy config keys (TTL, attempts, lockout, pepper)
  key_links:
    - from: utils/otp_security.py
      to: models/models.py
      via: OtpChallenge ORM write/read operations
      pattern: OtpChallenge\(
    - from: routes/auth.py
      to: utils/otp_security.py
      via: OTP helper imports for register/verify flow
      pattern: from utils\.otp_security import
---

<objective>
Establish the OTP security foundation for Phase 20 by introducing a server-side challenge model and cryptographic OTP helpers.

Purpose: Remove static OTP paths and guarantee hash-only storage primitives before route integration.
Output: New OTP challenge schema, migration script, config policy keys, and reusable OTP security module.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/20-otp-security-policy-core/20-RESEARCH.md
@models/models.py
@config.py
@database/migrate_anti_spam_phase2.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add OTP challenge model and cryptographic helper module</name>
  <read_first>models/models.py, config.py, routes/auth.py</read_first>
  <files>models/models.py, config.py, utils/otp_security.py</files>
  <behavior>
    - Issued OTP is always six digits and generated by cryptographically secure randomness.
    - OTP comparison runs against hash metadata with constant-time comparison.
    - Challenge metadata includes expiry and lockout policy fields.
  </behavior>
  <action>Add class OtpChallenge in models/models.py with fields: id, email, purpose, otp_hash, otp_salt, pepper_version, attempts_used, max_attempts, issued_at, expires_at, locked_until, used_at, invalidated_at, invalidation_reason, status. Add config keys in config.py: OTP_TTL_SECONDS default 300, OTP_MAX_ATTEMPTS default 5, OTP_LOCKOUT_SECONDS default 900, OTP_PEPPER from env (no literal secret), OTP_PEPPER_VERSION default v1. Create utils/otp_security.py implementing concrete functions generate_otp_code() using secrets.randbelow(900000)+100000, hash_otp(code, salt, pepper) using hashlib.pbkdf2_hmac, issue_otp_challenge(session, email, purpose, config), verify_otp_submission(challenge, submitted_code, now, config) with hmac.compare_digest and lifecycle result enum values.</action>
  <acceptance_criteria>
    - models/models.py contains class OtpChallenge(db.Model)
    - config.py contains OTP_TTL_SECONDS, OTP_MAX_ATTEMPTS, OTP_LOCKOUT_SECONDS, OTP_PEPPER, OTP_PEPPER_VERSION
    - utils/otp_security.py contains generate_otp_code, issue_otp_challenge, verify_otp_submission
    - utils/otp_security.py contains secrets.randbelow and hmac.compare_digest
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile models/models.py config.py utils/otp_security.py</automated>
  </verify>
  <done>OTP foundation types and helper functions exist and are syntax-valid.</done>
</task>

<task type="auto">
  <name>Task 2: Add manual migration script for otp_challenges table</name>
  <read_first>database/migrate_anti_spam_phase2.py, database/migrate_subscription.py, models/models.py</read_first>
  <files>database/migrate_otp_challenges.py</files>
  <action>Create database/migrate_otp_challenges.py using app.app_context() migration style. Script must ensure otp_challenges table exists with columns matching OtpChallenge model and create indexes on email, status, expires_at. Script must be idempotent: reruns should not fail if table/indexes already exist.</action>
  <acceptance_criteria>
    - database/migrate_otp_challenges.py exists
    - migration script checks inspector.get_table_names() for otp_challenges
    - migration script creates indexes for otp_challenges email/status/expiry lookups
    - script contains if __name__ == "__main__": migrate()
  </acceptance_criteria>
  <verify>
    <automated>python -m py_compile database/migrate_otp_challenges.py</automated>
  </verify>
  <done>Manual schema creation path exists and follows repository migration conventions.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> auth route | Untrusted OTP input enters verification path |
| auth service -> database | OTP challenge metadata persists to server storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | I | OTP challenge storage | mitigate | Store only otp_hash + otp_salt + pepper_version; never persist plaintext OTP |
| T-20-02 | S | OTP generation | mitigate | Use secrets-based generation to prevent predictable OTP values |
| T-20-03 | T | OTP verify compare | mitigate | Use hmac.compare_digest for constant-time hash validation |
</threat_model>

<verification>
- Run syntax validation for newly added model/config/security helper and migration scripts.
- Confirm no static OTP literal fallback remains in newly added OTP helper.
</verification>

<success_criteria>

- OtpChallenge schema and migration exist.
- OTP security helper module is importable and cryptographically grounded.
- Config keys required for Phase 20 security policy are defined.
</success_criteria>

<output>
After completion, create `.planning/phases/20-otp-security-policy-core/20-01-SUMMARY.md`
</output>
